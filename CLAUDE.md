# 쇼핑 쇼츠 작업 지침 — 이 폴더에서는 이대로만 한다

사용자가 소재 영상(과 있으면 판매 링크)을 주면 **아래 15단계를 순서대로 전부** 밟는다.
시간이 걸려도 끝까지 간다. 빠르게 하려고 합치거나 건너뛰지 않는다.

제작 규칙 원문은 `~/.claude/skills/shopping-shorts/SKILL.md` 에 있다.
**이 문서는 그 규칙을 지키게 만드는 지시다. 예외 없다.**

---

## 항상 지키는 것 (모든 단계에 적용)

1. **승인은 사용자가 한다.** 도구(`*.py`, `*.jsx`)와 문서(`SKILL.md`, 템플릿, 이 파일)를
   승인 없이 고치지 마라. 고칠 게 보이면 **무엇을·왜·어떻게 를 보고하고 멈춘다.**
2. **질문에는 답만 한다.** 사용자가 물으면 답한다. 답 대신 코드를 고치지 마라.
3. **한 번에 한 단계.** 여러 단계를 한 번에 묶어 실행하지 마라. 각 단계 끝에 보고한다.
4. **산출물은 표준 파일로만 보여준다.** `out/storyboard.png` · `out/refs/*.png` ·
   `out/prep_check/*.png` · `out/check/sheet.png`.
   임의로 쪼개거나 새 시트를 만들지 마라 — 번호 체계가 깨져 라벨과 대조가 안 된다.
5. **도구 출력은 마지막 줄까지 본다.** `grep` 으로 걸러 보다가 에러·트레이스백을 놓치지 마라.
6. **못 하면 멈추고 말한다.** 건너뛰고 진행하지 않는다. 모르면 사용자에게 묻는다.
7. **지어내지 않는다.** 확인 안 된 숫자(가격·용량·시간·온도·무게)는 쓰지 않는다.
   모르면 "미확인"으로 두고 그 수치를 대본과 카피킷에서 뺀다.
8. **받은 소재는 전부 쓴다.** 빼야 할 이유가 보이면 **빼지 말고 먼저 말한다.**
   빼는 건 사용자가 정한다.
9. **검사에서 FAIL 이 뜨면 데이터를 맞추지 마라.** 소재나 대본이 부족하다는 신호다.
   임계값을 낮추거나 채우기용 값을 넣지 마라.
10. **비용.** 유료 AI 영상 생성 금지(Higgsfield·Runway·Kling·Veo·Remotion Cloud·PAYG).
    ElevenLabs 는 무료 크레딧만. PAYG·Auto Top Up 켜지 마라.
    TTS 실패 시 **렌더하지 말고 멈춰서 보고**한다.
11. **파는 영상이다.** `"사지 마세요"` 같은 안티셀 금지. 판매원 톤도 금지.
12. **소재 권리는 사용자가 판단한다.** 제3자 영상이면 **처음 한 번만 알리고**,
    사용자가 진행하라고 하면 그대로 만든다. **매번 다시 묻지 마라.**
    권리자가 신고해서 내려야 하면 그때 내린다 — 그건 사용자가 감수하는 범위다.
13. **워터마크·남의 채널명은 화면에 남기지 않는다.**
    - 크롭으로 지울 수 있으면 `--keep-top` 으로 잘라내고 **쓴다**
    - 화면 중앙이라 못 지우거나, 잘라내면 구도가 망가지면 **그 소재는 뺀다**
    - 이건 권리 문제가 아니라 **품질 문제**다. 내 영상에 남의 브랜드가 나가면 안 된다
    - 우리 워터마크도 넣지 마라
14. **한 소재를 길게 이어 붙이지 마라.** 컷은 짧게, 소재는 번갈아 쓴다.
    - 컷 중앙값 **1.2~1.8초**, 개별 컷 **0.6~2.5초**
    - 같은 소재 **연속 5컷 이하**, 한 소재 비중 **75% 이하**
    - 한 소재를 길게 틀면 "그 영상을 그대로 튼 것"처럼 보인다

---

## 고정 설정 — 제품이 바뀌어도 건드리지 않는다

`remix.json` 에 들어가고 `new_project.py` 가 기본값으로 넣어준다.

| 값 | 설정 | 이유 |
|---|---|---|
| 목표 길이 | **15~30초** (권장 20~28) | 나레이션 길이가 영상 길이를 정한다 |
| 배속 | **1.2** | 실측 기준 |
| 자막 폰트 | **Jua** (주아체) | `font-weight` 는 400 고정. 합성 볼드 금지 |
| 광고 표시 | **우측 상단 상시** (`adBadge`) | 공정위 추천·보증 심사지침. 끄지 마라 |
| 썸네일 | 검정 박스 · 노랑 강조 · `size 118` · `top 330` | 13단계 참조 |

**포맷은 다섯 중 하나를 고른다** — 내돈내산 리뷰 / 언박싱 / 가성비 시연 /
꿀템 추천 / ASMR. 레퍼런스가 있으면 그 구조를 따른다.

---

# 15단계

## 1. 작업 폴더를 정리한다

이전 제품이 남아 있으면 보관하고 새로 시작한다. **손으로 폴더를 옮기지 마라.**

```
python new_project.py --archive <이전제품> --name "<새 제품 임시명>"
```

- 제품명을 아직 모르면 임시로 둔다. **3단계에서 확정한 뒤 고친다.**
- 지난 제품으로 돌아가려면 `--archive <지금것> --restore <지난제품>`

보고: 무엇을 어디로 보관했는지, 새 폴더가 비었는지.

## 2. 소재를 `raw/` 에 넣는다

받은 파일을 **전부** 복사한다. 파일명은 `a.mp4` `b.mp4` 처럼 짧게 바꾼다.

```
ffprobe -v error -select_streams v:0 -show_entries stream=width,height \
  -show_entries format=duration -of default=nw=1 raw/<파일>
```

보고: 파일별 해상도·길이·용량. 총 길이.

## 3. 🚧 소재를 먼저 본다 — 제품은 여기서 정한다

**파일명이나 링크만으로 제품을 단정하지 마라.** 번역된 파일명은 카테고리만 알려준다.

```
python <watch>/scripts/watch.py raw/<파일> --detail balanced --no-whisper --max-frames 14 --out-dir out/w_<이름>
python ref_sheet.py --parent out --glob "w_*" --out out/refs
```

**시트를 전부 열어서 본다.** 화면에 제품명이 박혀 있는 경우가 많다.

**`watch` 는 여기서 한 번만 돈다.** 뒤 단계에서 다시 돌리지 마라 —
같은 시트를 9단계 라벨과 10단계 대본에서 계속 참조한다.

보고:
- 제품이 무엇인지 (**어느 시트 몇 번 프레임에서 확인했는지 근거와 함께**)
- 소재별 0~1초에 뭐가 나오는지 (훅 후보)
- 동작 순서와 시각
- 결과 장면(머니샷) 위치
- **원본 자막·워터마크가 어디에 있는지** (없으면 "없음")
- **"우리 소재에 없는 컷" 목록**

확정한 제품명으로 `remix.json` 의 `product` 를 **즉시 고친다.**
`product` 는 **훅 문장에서 발음할 짧은 이름**이다(예: `폴딩박스`).

## 4. 판매 페이지를 확인한다

링크가 있으면 상품명·가격·용량·별점·리뷰를 가져온다.

- 쿠팡은 **서버 요청을 막는다(403). 로그인 여부와 무관하다.**
- 브라우저 창도 정책 차단, `coupang-product-search` 프록시도 죽어 있을 수 있다
- **네 경로가 다 막히면 그게 정상이다.** 사용자에게 상품명·가격·용량·리뷰를
  복사하거나 캡처해 달라고 **요청한다. 그게 정상 경로다.**
- 사용자가 못 주면 **"미확인"으로 두고 그대로 진행한다.** 지어내지 마라

`remix.json` 에 기록: `productFull`(정식 상품명) · `seller` · `price` · `link`.

## 5. 리뷰를 여섯 갈래로 읽는다

좋은 점만 찾지 마라. **실망 포인트를 먼저 제거하는 게 더 중요하다.**

| 분류 | 뽑을 것 | 쓰는 곳 |
|---|---|---|
| 구매 이유 | 왜 골랐는지 | 첫 3초 훅 |
| 만족 포인트 | 반복되는 장점 | 핵심 베네핏 |
| 사용 장면 | 누가 언제 | 컷 순서 |
| **불만 포인트** | 크기·소음·내구성·호환 | **과장 방지** |
| 비교 포인트 | 기존 제품 대비 차이 | **문제→해결 구조** |
| 반복 표현 | 자주 쓰는 단어 | 자막·카피 |

**핵심 소구점은 "비교 포인트"에서 나온다.** 카테고리 공통 기능이 아니라
**이 상품만의 차이**를 잡는다. `remix.json` 의 `keySelling` 에 **한 단어**로 적는다.

높은 별점만 있으면 불만 칸이 빈다. 그러면 **그 항목의 수치를 아예 쓰지 마라.**

## 6. 니치·타깃·트렌드를 정리한다

제품에서 카테고리·타깃을 도출하고 트렌드를 검색한다.
계절 상품이면 시즌을 확인해 보고한다.

보고: 니치 · 타깃 · 트렌드 요약 · 시즌 리스크.

## 7. 소재를 전처리한다

```
python prep_sources.py --dir raw --out sources                     # 자막 없을 때
python prep_sources.py --dir raw --out sources --keep-top <px>     # 하단 자막 제거
```

- 원본 자막이 있으면 3단계에서 잰 위치를 근거로 `--keep-top` 을 정한다
- 자막이 **화면 중앙**을 지나가면 크롭으로 못 없앤다. **그 소재는 쓸 수 없다고 보고**하고
  뺄지 사용자에게 묻는다. 임의로 빼지 마라
- 도구가 `out/prep_check/*.png` 를 강제로 뽑는다. **열어서 자막 잔상을 확인한다**
- `out/refs/` 가 비어 있으면 도구가 **실행을 거부한다** (3단계를 먼저 하라는 뜻).
  레퍼런스가 정말 필요 없을 때만 `--skip-ref` 로 넘긴다 — **함부로 쓰지 마라**

보고: 파일별 원본 해상도 → 1080x1920, 확대율, 잘라낸 px, **확인 시트 결과**.

## 8. 장면을 인덱싱한다

```
python index_sources.py --dir sources --cuts 18
```

보고: 소재별 장면 수, 총 장면 수, `out/storyboard.png` **원본 그대로** 보여준다.
**쪼개지 마라.** 크더라도 그대로 보낸다.

## 9. 🚧 라벨 게이트 — 사용자 확인을 받는다

`out/scene_index.json` 의 **모든** 장면에 `label` 을 채운다.

- **못 쓰는 장면은 라벨을 `[` 로 시작한다** — `[자막] …`, `[다른색] …`, `[검은화면] …`
  도구가 이 표시를 읽어 컷이 그 구간을 지나가면 경고한다
- 소재별 **"쓸 수 있는 구간"** 을 재서 보고한다 (자막 사이에 낀 짧은 구간은 못 쓴다)

보고: 라벨 표 · 비트 묶음 제안 · 쓸 수 있는 구간 · 소재 균형 전망.
**여기서 멈추고 사용자 확인을 받는다.**

## 10. 대본을 쓴다 — 화면에 있는 것만

기승전결. 1인칭 후기 해요체. **소재에 없는 동작을 문장으로 쓰지 마라**(3단계 "없는 컷" 참조).

지킬 것:
- 훅(첫 문장)에 `product` 단어가 들어간다. 지시대명사·질문형으로 시작 금지
- `keySelling` 은 훅이나 두 번째 문장에
- 합쇼체(`~합니다`) 금지. `~해봤어요/~하더라고요/저는 ~거든요`
- **`~거든요` 는 끝 두 문장에서만.** 중간에 쓰면 흐름이 끊긴다
- 같은 어미 3연속 금지
- 마지막 문장은 `~거든요` 로 받친 뒤 끝낸다
- **강조 단어는 `*단어*`** 로 적는다 — 그 단어만 노랑으로 나온다.
  문장당 한 곳, **두세 글자**. 열 문장이면 4~5개만. 전부 넣으면 강조가 사라진다
  길면 자막 조각 경계에 걸쳐 잘려 색이 안 칠해진다. 조사는 빼고 명사만 잡아라
- **CTA 는 댓글 기반** — 영상 하나를 유튜브·인스타·틱톡에 다 올린다.
  "링크 아래에" 금지. `"구매라고 댓글 남겨주시면 링크 보내드릴게요"` 형태

보고: 대본 전문 + **각 문장이 어느 화면에 붙는지 표로**.

## 11. 컷을 배분한다

`remix.json` 의 `beats` 에 적는다. **컷이 아니라 비트를 적는다.**

- **비트 개수 = 나레이션 문장 개수** (1:1, 다르면 도구가 멈춘다)
- `shots` 에 적은 장면 수 = 그 문장의 컷 수 (순환·채우기 없음)
- 장면 수는 문장 길이로 정한다 — `문장 길이 ÷ 장면 수` 가 **0.6~2.5초**
- **첫 컷은 머니샷으로.** 그 프레임이 썸네일이 된다

보고: 비트별 장면 번호 · 소재 비중 · 같은 소재 최대 연속.

## 12. 나레이션·자막·컷을 생성한다

```
python make_remix.py                # 대본이 같으면 음성 재사용(크레딧 절약)
python make_remix.py --force-tts    # 강제로 다시 받기
python make_remix.py --no-tts       # 대본·컷 검사만 (음성 안 만듦, 크레딧 0)
```

대본과 목소리 설정이 지난번과 같으면 **TTS 를 다시 부르지 않는다.**
검증하느라 크레딧을 태우지 않기 위해서다. 대본을 고쳤으면 자동으로 다시 받는다.
**대본을 고쳤는데 "재사용" 이 뜨면 음성이 옛것이다** — 확인하고 `--force-tts` 로 다시 받아라.

도구가 9가지를 검사한다 — 훅 지시대명사 / 훅에 제품 / 소구점 위치 / 1인칭 비율 /
합쇼체 / 중간 `~거든요` / 어미 3연속 / 마지막 문장 하강 / 지난 제품 문구 재사용.

**`[경고]` 가 뜨면 렌더 전에 해결한다.** 경고를 안고 넘어가지 마라.

보고: 컷 수·길이 범위·평균, 나레이션 길이, 경고 유무.

## 13. 썸네일 문구를 정한다 — 후보를 뽑아 비교한다

**하나만 만들지 마라. 4~6개를 뽑아 각각 렌더해서 나란히 보여준다.**

각도를 바꾼다 — **문제 찌르기 ★★★ / 불가능해 보이기 ★★★ / 의심 ★★ / 결과 ★**
(결과형 `~했더니 ~됐다` 는 궁금할 게 없어 제일 약하다)

고르는 기준:
- **궁금증이 생기는가**
- **문구와 그림이 합쳐서 무슨 카테고리인지 아는가**
  (증명 장면을 쓸 때 특히 주의 — 매단 물건 이름만 쓰면 그 물건 거치대로 읽힌다)
- 확인 안 된 숫자가 없는가
- 두 줄 · 한 줄 8~10자 · 강조 한 곳
- **지난 제품 문구를 재사용하지 마라** (도구가 대조해 경고한다)

규격은 **고정**이다. 건드리지 마라 — 검정 박스 `#0D0D0D` · 노랑 강조 `#FFD84D` ·
`size 118` · `top 330` · `coverSec 0.7` · `inVideo false`.
제품마다 바뀌는 건 `text` 와 `cut` 뿐이다.

고른 뒤 `headline.text` 와 `headline.cut` 을 `remix.json` 에 적는다.

## 14. 렌더하고 썸네일을 뽑는다

```
npx remotion render src/index.jsx Remix output/shopping_shorts_remix.mp4 --crf 18
python thumb.py
```

썸네일은 **영상 맨 앞 0.7초 커버 프레임과 같은 그림**이다. 따로 합성하지 않는다.

## 15. 🚧 검수하고 카피킷을 만든다

```
python check_render.py --shots <컷수>
```

19항목을 검사한다 — 길이 / 해상도 / 오디오 트랙 / 오디오 길이 / 용량 /
컷 중앙값 1.2~1.8초 / 극단 컷 없음 / 연속 중복 장면 / 소재 편중 75% /
같은 소재 연속 5컷 / 자막↔음성 같은 대본 / 문장 수 / 나레이션 길이 /
썸네일 문구 / 썸네일 최신 / 문장 사이 조용함 / 카피킷 고지 / 카피킷 링크 / 검은 프레임.

**`out/check/sheet.png` 를 눈으로 확인한다.** 자동 검사가 다 통과해도
원본 자막 잔상·자막 잘림은 시트로만 잡힌다.

카피킷은 **플랫폼별로** 쓴다. `output/카피킷.md`:

| 플랫폼 | 링크를 어디에 | 본문 외에 켤 것 |
|---|---|---|
| 유튜브 쇼츠 | 설명란 + 고정 댓글 | "유료 프로모션 포함" 체크 |
| 인스타 릴스 | 프로필 상단 링크 + 댓글 자동 DM | "유료 파트너십 라벨" |
| 틱톡 | 프로필 바이오 + 댓글 답글 | "브랜디드 콘텐츠" 토글 |

**파트너스 고지를 각 플랫폼 블록 안에 넣는다.** 체크리스트에만 적으면 안 된다 —
복사하는 건 블록이지 체크리스트가 아니다. 자동 DM·답글 메시지에도 넣는다.

    쿠팡 파트너스 활동을 통해 일정액의 수수료를 제공받습니다.

카피킷 끝에 **업로드 규칙 · 하지 말아야 할 것 · 최종 체크리스트**를 붙인다.

마지막에 `out/preview.mp4` 압축본을 만들어 영상·썸네일·카피킷을 함께 전달한다.

---

## 자주 나는 사고 (전부 실제로 당한 것)

| 사고 | 막는 법 |
|---|---|
| 파일명만 보고 제품을 단정 | 3단계에서 시트를 먼저 연다 |
| 소재에 없는 동작을 대본에 씀 | 3단계 "없는 컷 목록"을 대본 전에 본다 |
| 원본 자막이 컷에 딸려 나옴 | 못 쓰는 장면 라벨을 `[` 로 시작 |
| 컷 경계 1프레임 검은 화면 | 프레임 단위로 맞물리게 (도구가 처리) |
| 타임스탬프 역전으로 음성이 잘림 | 합성 시 단조 증가 복구 (도구가 처리) |
| 검사 통과하려고 채우기용 장면 삽입 | FAIL 은 소재 부족 신호다. 대본을 줄여라 |
| `grep` 으로 걸러 보다 에러를 놓침 | 출력 마지막 줄까지 본다 |
| 소재를 임의로 제외 | 전부 쓴 버전을 먼저 만들고, 뺄지는 사용자가 정한다 |
| 카피킷 고지가 한 플랫폼에서 누락 | 블록 안에 넣는다. 도구가 섹션별로 검사 |
| 지난 제품 문구 재사용 | 제품마다 후보를 새로 뽑는다 |

## 작업 폴더

| 경로 | 역할 |
|---|---|
| `raw/` | 받은 원본. 손대지 않는다 |
| `sources/` | 전처리된 1080x1920. 인덱싱·컷 매핑 기준 |
| `public/sources/` | Remotion 이 읽는 사본. **`make_remix.py` 가 자동 동기화** |
| `public/narration.mp3` | 나레이션 단일 출처 |
| `out/` | 중간 산출물 — 컷 시트·스토리보드·인덱스·자막·검수 시트 |
| `output/` | 최종 영상 · 썸네일 · 카피킷 |
| `projects/<제품>/` | 끝난 제품 보관 |

공유 저장소(`../쇼츠_공유`)에 반영하려면:

```
python sync_share.py           # 뭐가 다른지 확인
python sync_share.py --apply   # 복사
```
