# 처음부터 따라하기 — 초보자용

컴퓨터에 아무것도 없는 상태에서 **쇼핑 쇼츠 첫 영상**이 나올 때까지의 전체 순서입니다.
위에서부터 순서대로 하나씩 하면 됩니다. 이미 된 단계는 건너뛰세요.

> 이 저장소(도구·규칙)는 이미 완성돼 있습니다. 여러분이 할 일은
> **① 프로그램 설치 → ② 저장소 받기 → ③ ElevenLabs 키 연결 → ④ 스킬 등록 → ⑤ 소재 준비 → ⑥ 프롬프트 붙여넣기** 입니다.

---

## 1. 프로그램 설치

### Windows

PowerShell 을 열고 한 줄씩 실행합니다.

```
winget install Python.Python.3.13
winget install OpenJS.NodeJS.LTS
winget install Git.Git
winget install Gyan.FFmpeg
winget install yt-dlp.yt-dlp
```

### Mac

터미널을 열고 실행합니다. (Homebrew 가 없다면 https://brew.sh 의 설치 명령을 먼저)

```
brew install node git ffmpeg yt-dlp python
```

### 확인

터미널을 **완전히 껐다가 다시 켠 뒤** 아래를 실행합니다. 전부 버전이 나오면 성공입니다.

```
python --version
node --version
git --version
ffmpeg -version
yt-dlp --version
```

> Windows 에서 `python` 이 스토어 창을 띄우면: 설정 → 앱 → 고급 앱 설정 →
> 앱 실행 별칭에서 `python.exe` 별칭을 끄세요.
> Mac 은 `python` 대신 `python3` 를 씁니다.

---

## 2. Claude Code 설치

```
npm install -g @anthropic-ai/claude-code
```

설치 후 아무 폴더에서 `claude` 를 실행하면 로그인 안내가 나옵니다. Claude 계정으로 로그인하세요.

---

## 3. 이 저장소 받기

```
git clone https://github.com/qwmll19-cmd/shopping-shorts.git
cd shopping-shorts
npm install
```

`npm install` 은 몇 분 걸립니다. 영상 렌더에 쓰는 Remotion 이 이때 설치됩니다.

> Remotion 은 개인·직원 3명 이하 회사는 무료입니다(상업용 영상 제작 포함).
> 그보다 큰 회사라면 THIRD-PARTY.md 를 읽어보세요.

---

## 4. ElevenLabs 키 발급·연결 (나레이션 음성)

### 4-1. 키 발급

1. https://elevenlabs.io 접속 → 회원가입(무료)
2. 왼쪽 아래 프로필 → **API Keys**
3. **Create API Key** → 이름 아무거나(예: shorts) → 생성
4. 나온 키(`sk_...`)를 복사 — **이 화면을 벗어나면 다시 볼 수 없으니 지금 복사하세요**

### 4-2. 프로젝트에 연결

저장소 폴더의 `.env.example` 파일을 복사해서 이름을 `.env` 로 바꾸고, 메모장으로 열어 키를 붙여넣습니다.

```
ELEVENLABS_API_KEY=sk_여기에_복사한_키
```

### 4-3. 절대 하지 말 것

- 키를 **채팅창·GitHub·카톡에 붙여넣지 마세요.** 남이 내 크레딧을 씁니다.
- ElevenLabs 사이트에서 **PAYG(사용량 과금)·Auto Top Up 을 켜지 마세요.**
  무료 크레딧이 떨어지면 그냥 멈추는 게 안전합니다.
- 이 저장소의 도구는 대본이 같으면 음성을 재사용해서 크레딧을 아낍니다.
  대본이 확정되기 전에 여러 번 뽑지 마세요.

---

## 5. 스킬 등록

### 5-1. watch 스킬 (영상 분석 — 필수)

Claude 는 원래 영상을 못 봅니다. watch 스킬이 영상을 프레임과 대사로 바꿔 보여줍니다.

저장소 폴더에서 `claude` 를 실행하고, **Claude Code 안에서** 한 줄씩 입력합니다.

```
/plugin marketplace add bradautomates/claude-video
/plugin install watch@claude-video
```

끝나면 Claude Code 를 껐다가 다시 켭니다.

**테스트** — Claude Code 안에서:

```
/watch https://www.youtube.com/watch?v=아무영상 이 영상 장면 순서를 표로 정리해줘
```

### 5-2. shopping-shorts 스킬 (쇼츠 제작 규칙 — 필수)

이 저장소의 `SKILL.md` 가 제작 규칙 전체입니다. 홈 폴더의 스킬 위치에 복사합니다.

Windows (PowerShell):
```
New-Item -ItemType Directory -Force "$env:USERPROFILE\.claude\skills\shopping-shorts"
Copy-Item SKILL.md "$env:USERPROFILE\.claude\skills\shopping-shorts\SKILL.md"
```

Mac:
```
mkdir -p ~/.claude/skills/shopping-shorts
cp SKILL.md ~/.claude/skills/shopping-shorts/SKILL.md
```

Claude Code 를 다시 켜면 "쇼핑 쇼츠 만들어줘" 라는 말에 이 스킬이 자동으로 잡힙니다.

---

## 6. 소재 영상 구하기 (샤오홍슈 예시)

쇼츠는 **소재 영상을 재편집**해서 만듭니다. 제품이 실제로 나오는 영상이 2~3개 필요합니다.

### 6-1. 샤오홍슈(小红书)에서 링크 추출

1. 샤오홍슈 앱(또는 https://www.xiaohongshu.com)에서 제품 영상을 찾습니다
2. 영상 오른쪽 아래 **공유(分享) 버튼** → **링크 복사(复制链接)**
3. 복사된 링크는 `http://xhslink.com/...` 같은 짧은 주소입니다

### 6-2. 다운로드

복사한 링크를 다운로더 사이트에 붙여넣으면 워터마크 없는 mp4 를 받을 수 있습니다.
아래 중 아무거나 쓰면 됩니다 (전부 무료, 접속만 하면 됨):

- https://tools.emmmm.dev/xiaohongshu
- https://www.tapsave.net/xiaohongshu/
- https://www.52gj.com/en/xiaohongshu

사용법은 다 같습니다: **링크 붙여넣기 → 다운로드 버튼 → mp4 저장.**
사이트가 막히면 "xiaohongshu downloader" 로 검색하면 비슷한 사이트가 계속 나옵니다.

유튜브·틱톡 영상은 터미널에서 yt-dlp 로 받습니다:

```
yt-dlp "영상주소"
```

### 6-3. ⚠️ 권리 확인 — 중요합니다

- **내 최종 영상에 넣어도 되는 소재**: 판매자·제조사가 제공한 영상, 직접 촬영한 영상
- **분석만 하고 넣으면 안 되는 소재**: 남이 올린 영상 (권한 확인 전까지)

남의 영상을 그대로 잘라 붙여 올리면 저작권 문제가 생길 수 있습니다.
쿠팡 파트너스로 수익을 붙일 영상이라면 더더욱 **판매자 제공 소재**를 쓰세요.
(판매자 상세페이지·공식 홍보 영상을 쓰고 싶으면 판매자에게 문의해 허락을 받는 게 확실합니다.)

받은 영상은 저장소 폴더의 `raw/` 에 넣습니다. 파일명은 `a.mp4`, `b.mp4` 처럼 간단하게 바꾸세요.

---

## 7. 첫 영상 만들기 — 프롬프트 붙여넣기

여기까지 됐으면 이제 Claude 가 다 합니다.

1. 저장소 폴더에서 `claude` 실행
2. `프롬프트_템플릿.md` 를 열어 **전체판**을 복사
3. 대괄호 `[ ]` 부분만 채웁니다 — 제품명, 소재 파일 경로, 판매 링크, 소구점
4. Claude Code 에 붙여넣기

그러면 Claude 가 0~8단계를 순서대로 진행합니다:

```
0. 제품·트렌드 리서치  →  1. 소재 분석(멈춰서 확인받음)  →  2. 전처리
→  3. 스토리보드  →  4. 장면 라벨(멈춰서 확인받음)  →  5. 대본
→  6. 컷 배치  →  7. 나레이션·렌더  →  8. 19항목 자동 검수 + 업로드 카피
```

중간에 두 번 멈춰서 확인을 요청합니다. 스토리보드와 라벨을 보고 "진행해" 라고 하면 됩니다.

완성되면 이렇게 나옵니다:

| 파일 | 내용 |
|---|---|
| `output/shopping_shorts_remix.mp4` | 완성 영상 (1080x1920) |
| `output/썸네일.png` | 썸네일 이미지 |
| `output/카피킷.md` | 유튜브·인스타·틱톡 업로드 문구 (파트너스 고지 포함) |

---

## 8. 자주 나는 오류

| 증상 | 해결 |
|---|---|
| `command not found: claude` | 2단계 다시. 터미널 껐다 켜기 |
| `python` 이 스토어 창을 띄움 (Windows) | 1단계 끝의 앱 실행 별칭 끄기 |
| `ffmpeg not found` | 1단계의 ffmpeg 설치 후 터미널 재시작 |
| ElevenLabs 401/인증 오류 | `.env` 의 키 앞뒤 공백·따옴표 제거, 키 만료 확인 |
| ElevenLabs 402 | 무료 크레딧 소진 — 다음 달까지 대기하거나 대본 줄이기 |
| watch 가 한글 Windows 에서 `UnicodeDecodeError` | Claude 에게: "watch 스킬의 frames.py·whisper.py 의 subprocess 호출에 `encoding='utf-8', errors='replace'` 를 추가해줘" |
| watch 가 `-vsync` 오류 | Claude 에게: "watch 스킬의 `-vsync` 를 `-fps_mode` 로 바꿔줘" (최신 ffmpeg 에서 삭제된 옵션) |
| `1단계(레퍼런스 분석)를 안 했습니다` | 건너뛴 게 아니라 정상 동작 — watch 분석부터 하라는 뜻 |
| 검수에서 FAIL | **억지로 통과시키지 말 것.** 소재나 대본이 부족하다는 신호 (SKILL.md 6-B) |

---

## 9. 두 번째 영상부터

```
python new_project.py --archive 첫제품이름 --name "새 제품"
```

이전 작업이 `projects/` 에 통째로 보관되고 새로 시작됩니다. 돌아가고 싶으면:

```
python new_project.py --archive 지금것 --restore 첫제품이름
```
