# 쇼핑 쇼츠 제작 파이프라인 — 설치 안내

한국어 쇼핑 쇼츠(커머스 숏폼)를 소재 영상에서 자동 재편집으로 만드는 도구 모음입니다.
유료 AI 영상 생성은 쓰지 않습니다. ffmpeg + Remotion 로컬 렌더 + ElevenLabs TTS 만 씁니다.

---

## 1. 필요한 것

| | 용도 | 설치 |
|---|---|---|
| **Python 3.10+** | 도구 전체 | python.org |
| **Node.js 18+** | Remotion 렌더 | nodejs.org |
| **ffmpeg / ffprobe** | 영상 처리 | `winget install Gyan.FFmpeg` 또는 ffmpeg.org |
| **ElevenLabs 계정** | 나레이션 | elevenlabs.io (무료 크레딧으로 시작 가능) |
| **yt-dlp** (선택) | 레퍼런스 영상 받기 | `winget install yt-dlp` |

**PATH 확인** — 아래가 전부 버전을 출력해야 합니다.

```bash
python --version
node --version
ffmpeg -version
```

Claude Code 세션에서 명령을 못 찾으면 `~/.claude/settings.json` 의 `env.PATH` 에
ffmpeg·node·python 경로를 직접 넣어 주세요.

---

## 2. 설치

```bash
npm install
```

`.env.example` 을 `.env` 로 복사하고 ElevenLabs API 키를 넣습니다.

```
ELEVENLABS_API_KEY=여기에_본인_키
```

> ⚠️ **`.env` 는 절대 공유하지 마세요.** 키가 유출되면 남이 내 크레딧을 씁니다.
> `.gitignore` 에 이미 제외돼 있습니다.

**비용 주의** — ElevenLabs 대시보드에서 **PAYG·자동 충전을 켜지 마세요.**
무료 크레딧만 쓰다가 소진되면 멈추게 하는 것이 안전합니다.

---

## 3. 스킬 등록 (Claude Code 를 쓸 때)

`SKILL.md` 를 아래 위치에 넣으면 Claude 가 규칙을 읽고 8단계를 따라갑니다.

```
~/.claude/skills/shopping-shorts/SKILL.md
```

1단계(레퍼런스 분석)에는 `watch` 스킬이 필요합니다. 별도 설치하세요.
없이 쓰려면 `make_remix.py --skip-ref` 로 넘길 수 있지만 **권장하지 않습니다** —
소재의 동작 순서를 모르고 대본을 쓰면 말과 화면이 어긋납니다.

---

## 4. 첫 영상 만들기

```bash
python new_project.py --name "제품명"          # 폴더 만들고 remix.json 생성
# raw/ 에 소재 영상(mp4)을 넣습니다
python prep_sources.py --dir raw --out sources  # 1080x1920 으로 굽기
python index_sources.py --dir sources --cuts 18 # 장면 인덱싱 + 스토리보드
# out/storyboard.png 를 보고 out/scene_index.json 의 label 을 전부 채웁니다
# remix.json 에 product · keySelling · headline.text · narration · beats 를 씁니다
python make_remix.py                            # 나레이션·자막·컷 배치
npx remotion render src/index.jsx Remix output/완성본.mp4 --crf 18
python thumb.py                                 # 썸네일
python check_render.py --shots 18               # 19항목 자동 검수
```

`프롬프트_템플릿.md` 에 Claude 에게 붙여넣을 요청 양식이 있습니다.
**`SKILL.md` 를 먼저 읽어보세요.** 규칙이 전부 거기 있습니다.

---

## 5. 자주 쓰는 명령

```bash
python make_remix.py --no-tts       # 대본·컷 검사만 (음성 안 만듦, 크레딧 0)
python make_remix.py --force-tts    # 대본이 같아도 음성 다시 생성
python new_project.py --archive <이전제품> --name "<새 제품>"
python new_project.py --archive <지금것> --restore <지난제품>
python thumb.py --cut 5             # 다른 컷으로 썸네일
```

대본과 목소리 설정이 지난번과 같으면 TTS 를 자동으로 재사용합니다(크레딧 절약).

---

## 6. 소재 권리

**제3자 영상을 최종 상업용 영상에 넣지 마세요.** 사용 권한이 확인된 소재
(판매자 제공 · 직접 촬영)만 쓰고, 남의 영상은 구조 분석용으로만 보세요.

쿠팡 파트너스 링크를 쓰면 **모든 플랫폼 본문에** 고지 문구를 넣어야 합니다.
카피킷이 자동으로 넣어주고 `check_render.py` 가 누락을 잡습니다.

---

## 7. 안 될 때

| 증상 | 원인 |
|---|---|
| `1단계(레퍼런스 분석)를 안 했습니다` | `out/refs/` 가 비어 있음. watch → ref_sheet.py 를 먼저 |
| `한글 폰트를 찾지 못했습니다` | `public/fonts/` 에 ttf 가 있는지 확인 |
| TTS 가 402 를 냄 | 무료 크레딧 소진 또는 유료 전용 보이스 |
| 렌더가 검은 화면 | `public/sources/` 동기화 문제 — `make_remix.py` 를 다시 돌리면 자동 복구 |

검수에서 FAIL 이 뜨면 **데이터를 억지로 맞추지 마세요.** 소재나 대본이 부족하다는 신호입니다.
자세한 내용은 `SKILL.md` 의 6-B 절에 있습니다.
