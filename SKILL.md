---
name: shopping-shorts
description: >
  한국어 쇼핑 쇼츠(커머스 숏폼)를 기획부터 완성 영상까지 만든다. REMIX 모드가 기본 —
  사용자가 가진 실제 동영상을 장면 단위로 인덱싱해 15~30초 세로 쇼츠로 재편집한다.
  ffmpeg + Remotion 로컬 렌더 + ElevenLabs TTS 만 쓰고 유료 AI 영상 생성(Higgsfield·Runway·
  Kling·Veo·Remotion Cloud)은 쓰지 않는다.
  포함: 제품·니치 감지, 트렌드 리서치, 레퍼런스 벤치마킹(watch), 기승전결 대본,
  한국어 원어민 나레이션, 글자 단위로 맞춘 자막, 컷 편집·크롭·punch-in, 1080x1920 렌더,
  완성본 16항목 자동 검수, 업로드 카피 7종(썸네일·제목·스크립트·유튜브 설명·SEO 태그·틱톡·인스타).
  트리거: '쇼핑 쇼츠 만들어줘', '커머스 숏폼', '내돈내산 리뷰 영상', '언박싱 쇼츠',
  '이 영상처럼 만들어줘', '이거 벤치마킹해서', '이 상품들로 각각 만들어줘',
  '쿠팡 파트너스 영상', '제품 리뷰 영상', 제품 링크·영상 파일 + 콘텐츠 요청.
---

# 쇼핑 쇼츠 제작 (REMIX 모드)

한국 쇼핑 쇼츠 6편(같은 제품, 조회수 1.6만~793만)을 프레임 단위로 실측 대조해 얻은 규칙이다.

## 0. 대전제 — 이건 파는 영상이다

**절대 "사지 마세요"로 열지 마라.** 반박 심리를 노린 안티셀은 판매 목적 쇼츠에서 쓰지 않는다.
과거에 이 방식으로 만들었다가 사용자에게 두 번 거부당했다. 판매 영상에서 제품을 깎는 문장은
어떤 형태로든 넣지 않는다.

문제 제기는 하되 **제품이 아니라 상황을 깎는다.**
- ✅ "집에서 만들려면 얼음 가는 기계가 있어야 했거든요" (기존 방법의 불편)
- ❌ "이거 사지 마세요" / "안 사도 되는 조건 세 가지"

## 1. 말투 — 1인칭 후기, 해요체 (판매원 톤 금지)

**가장 중요한 것: 파는 사람이 아니라 써 본 사람이 말한다.**
상세페이지 문장을 그대로 읽으면 3초 만에 광고로 분류된다.

| ❌ 판매원 톤 (상세페이지) | ✅ 1인칭 후기 톤 |
|---|---|
| 벽에 구멍 안 뚫는 드라이기 거치대**입니다** | 구멍 안 뚫어도 되는 거치대 **사봤어요** |
| 설치가 **간편합니다** | 붙이기만 하면 돼서 **진짜 편했어요** |
| 선을 정리**할 수 있습니다** | 선 감아두니까 **깔끔하더라고요** |
| 수납 공간이 **있습니다** | 화장품도 **올려두고 있어요** |
| 내구성이 **뛰어납니다** | 생수병 **매달아봤는데 끄떡없더라고요** |

**후기 표지를 문장의 절반 이상에 넣어라** — `저는 / 제가 / 우리 집 / ~더라고요 /
~했어요 / ~봤어요 / ~거든요 / 샀어요`. `make_remix.py` 가 비율을 세서 50% 미만이면 경고한다.

**나레이션은 대본 전체를 한 번에 읽힌다.** 문장마다 따로 TTS 를 부르면 ElevenLabs 가 각각을
독립된 발화로 처리해 문장마다 억양이 리셋된다. 이어붙이면 토막처럼 들린다.
`with-timestamps` 엔드포인트가 문자 단위 시각을 주므로 문장 경계는 그걸로 자른다.

**중간 문장을 `~거든요`로 닫지 마라.** `~거든요`는 말을 **닫는** 종결어미다.
다음 문장이 전환·해결이면 거기서 뚝 끊겨 두 문장이 따로 논다.

| ❌ 끊긴다 | ✅ 이어진다 |
|---|---|
| "…계속 신경 쓰**였거든요.**" → "이건 …끝이더라고요." | "…계속 신경 쓰**였는데,**" → "이건 …끝이더라고요." |

쓸 수 있는 자리는 **끝 두 문장뿐이다** (마무리 억양을 받쳐주는 자리, §2-J).

**첫 문장도 예외가 아니다.** 훅 다음에도 이야기가 이어지므로 똑같이 끊긴다.
훅은 `~사봤는데요,` 처럼 연결어미로 끝내고 다음 문장으로 넘긴다.

    ✅ "…되는 〈제품〉 사봤는데요," → "원래 〈불편〉했는데,"
    ❌ "…되는 〈제품〉 사봤거든요." → "원래 〈불편〉했는데,"

`make_remix.py` 가 몇 번째 문장인지 찍어서 경고한다.

**끝맺음 금지어:** `~입니다 / ~합니다 / ~할 수 있습니다 / ~가 가능합니다 / 활용도가 높습니다`


딱딱한 설명문은 광고로 읽힌다. **"~습니다"를 쓰지 마라.**

| ❌ 딱딱함 | ✅ 자연스러움 |
|---|---|
| 슬러시가 **됩니다** | 슬러시가 **됐어요** |
| 여름마다 **생각나죠** | 여름마다 진짜 **땡기잖아요** |
| 기계가 있어야 **했습니다** | 기계가 있어야 **했거든요** |
| 아이스**팩입니다** | 아이스**팩이에요** |
| 확인해 **보세요** | 한번 보고 **가세요** |

- **종결은 ~요/~잖아요/~거든요/~고요.**
- **시제는 후기형.** "됩니다"(설명) 말고 "됐어요"(직접 해본 사람). 첫 문장에서 특히 중요하다.
- **의성어를 쓴다.** 조물조물, 꾹꾹, 톡 — 레퍼런스 히트작들이 실제로 쓴 표현이다.

## 2. 조회수를 가른 4가지

| | 히트작 (22만~793만) | 실패작 (1.6만~2.7만) |
|---|---|---|
| 첫 1초 | **결과물부터** 보여줌 | "…알고 계신가요?" 질문, 또는 CTA 자막 |
| 앵글 | 제품이 아니라 **상황** (소주를 얼린다 / 70만원 아꼈다) | 기능 나열 (얼음물·우유·커피·육수…) |
| CTA | **맨 끝 1~2초**에만 | 첫 컷부터 "지금 쿠팡 들어가서" |
| 화자 | 어설퍼도 사람 목소리, 지저분한 식탁 | 매끈한 템플릿 광고 톤 |

**신뢰 장치:** 단점 하나를 짧게 인정하고 **곧바로 장점으로 회수**한다.
(내돈내산 리뷰라면 실패를 끝까지 보여줘도 되지만, 판매 목적이면 회수 없는 단점은 넣지 않는다.)

**길이는 변수가 아니다.** 14초(46만)와 59초(793만)가 둘 다 성공했다. 매 초에 이유가 있는지가 변수다.

## 2-A. 훅 — 첫 3초에 반드시 들어갈 것

쇼츠는 앞 3초에서 갈린다. 그 3초에 **두 가지**가 없으면 스크롤된다.

1. **뭘 파는지** — 제품이 무엇인지. 훅에 제품 명사가 없으면 안 된다
2. **왜 봐야 하는지** — 이 제품만의 차별점. 카테고리 1순위 소구점을 여기 쓴다

실제로 이렇게 틀린 적이 있다:

> ❌ "이렇게 걸어두니까 욕실이 확 정리되더라고요."
>
> - **지시대명사로 시작** — "이렇게"가 뭘 가리키는지 모른다
> - **주어가 드라이기** — 파는 건 거치대인데 "욕실이 정리되는 드라이기"로 읽힌다
> - **핵심 소구점(무타공)이 5번째 문장** — 트렌드·레퍼런스가 전부 미는 걸 뒤로 미뤘다

> ✅ "벽에 구멍 안 뚫어도 되는 드라이기 거치대 사봤어요."
>
> 차별점(구멍 안 뚫음) + 제품(드라이기 거치대) + 1인칭(사봤어요)

**금지:** 지시대명사(`이렇게 / 이거 / 이게 / 그렇게`)로 시작하기 · 질문형(`…알고 계신가요?`)

`remix.json` 에 `product`(제품명)와 `keySelling`(핵심 소구 키워드)을 적어두면
`make_remix.py` 가 훅에 그 둘이 들어갔는지, 소구점이 뒤로 밀리지 않았는지 검사한다.

## 2-B. 제품 우선 원칙 (강제)

쇼핑 쇼츠의 주인공은 **사람이 아니라 제품**이다.

- **화면 시간 70% 이상을 제품**이 차지해야 한다. 인물 얼굴이 매 컷에 나오는 구성은 실패다.
- 인물 등장은 이 순서로 고른다 — **① 손·팔만**(기본) → ② 신체 일부(어깨 너머·무릎 위) →
  ③ 목소리만 → ④ 아주 짧게 얼굴(리액션 1~2초, 전체의 20% 이내).
- 컷은 **제품이 뭘 하는지**(작동·설치·변화·전후)부터 짜고, 인물은 그 제품을 만지는 손으로만 최소 삽입한다.
  "사람이 제품 들고 설명하는 토킹헤드"는 기본값이 아니다.
- **매크로·클로즈업을 적극 쓴다.** 제품 표면, 작동 부위, 변화하는 순간의 익스트림 클로즈업이
  스크롤을 멈추게 하는 지점이다. (실측 근거: 793만 영상의 핵심 컷도 "벽면부터 어는" 매크로였다.)

## 2-C. 포맷 5종 — 하나를 골라 컷을 설계한다

| 포맷 | 바이브 | 프레이밍 |
|---|---|---|
| **내돈내산 솔직리뷰** | "내 돈 주고 산" 신뢰형. 장단점 솔직 평가 | 제품 든 손 + 클로즈업. 얼굴은 판정 리액션 1~2초 |
| **언박싱 / 하울** | 택배 뜯는 순간의 설렘 | 탑다운 앵글, **손만으로 완결** |
| **가성비 비교 / 시연** | "이 가격 실화?" 또는 비포&애프터 | 인물 거의 없음. 제품이 일하는 순간이 화면 전체 |
| **꿀템 추천 / 생활 배치** | "이건 진짜 사야 해" | 제품이 놓인 공간 + 제품. 인물은 스쳐가거나 손만 |
| **ASMR 언박싱 / 작동음** | 소리 주도 클로즈업 | 인물 등장 없음. 매크로 렌즈로 붙어서 |

> **레퍼런스가 있으면 5종 분배를 쓰지 않는다.** 균등 분배는 레퍼런스가 없을 때의 기본값이다.
> 벤치마킹 영상이 주어지면 **그 영상의 구조가 곧 포맷**이다.

컨셉 시드 예시(같은 포맷 안에서도 겹치지 않게 변형):
- 리뷰 — 마감·재질 클로즈업 / 스펙 라벨 매크로 → 작동 검증 / 경쟁템과 나란히 비교 /
  한 달 사용감 / **단점 하나를 짚고 그래도 쓰는 이유**
- 언박싱 — 테이프 가르기 / 완충재 걷어내며 슬로우 리빌 / 구성품 정렬 / 표면 쓸어보기
- 시연 — 사용 전 → 작동 → 사용 후(**같은 앵글 고정**) / 작동 부위 익스트림 클로즈업 /
  버튼 누르자 반응하는 순간

## 2-D. 업로드 카피 킷 — 영상만 뽑고 끝내지 마라

영상 파일은 절반이다. **업로드에 필요한 카피 7종을 같이 만들어 `output/카피킷.md` 로 저장한다.**

> **① 썸네일 → ② 제목 → ③ 쇼츠 스크립트 → ④ 유튜브 설명 → ⑤ SEO 태그 10개 → ⑥ 틱톡 → ⑦ 인스타그램**

1. **썸네일** — 10~12자. 숫자·금액으로 클릭 유도 (예: "3만원짜리 창문 혁명")
2. **제목** — 10~12자. 숫자·금액, 강한 궁금증
3. **스크립트** — `remix.json` 의 나레이션이 곧 이것. 구어체, 첫 3초 후킹,
   **현실적 상황 → 갈등 → 해결 → 혜택 → CTA**
4. **유튜브 설명** — 제품 키워드 2~3회 자연스럽게 + 끝에 태그 5개
5. **SEO 태그 10개** — 콤마 구분. 제품·니치에 맞게 매번 새로 뽑는다 (고정 태그 강제 금지)
6. **틱톡** — 짧은 후킹 + CTA. 링크 안내는 `[틱톡_링크안내]` 자리표시자
7. **인스타그램** — 짧은 후킹 + CTA. `[인스타_링크안내]` 자리표시자

쿠팡 파트너스면 설명란에 **광고 고지**를 반드시 넣는다:
`[광고] 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다.`

## 2-E. 레퍼런스에서 뽑을 네 가지

벤치마킹할 때 아래를 **전부** 확보한다. 하나라도 비면 이후가 어긋난다.

1. **총 길이(초)** — 우리 영상의 목표 길이
2. **장면 타임라인** — 몇 초에 무엇이, 각 장면 길이·앵글·거리·피사체·전환 지점
3. **원고 전문** — 시간 순 대사/내레이션
4. **설득 구조** — 첫 몇 초를 뭘로 잡는지, 문제→해결→혜택→CTA 가 몇 초 지점인지

**대사는 압축한다.** 원고 전문을 그대로 읽히면 말이 빨라지고 뭉갠다.
컷 하나에 **1~2문장, 25~40자 안쪽**. 나머지는 **말 대신 화면으로** 전달한다.
설명조·스펙 나열은 대사에서 버린다.

> ❌ "이사 왔는데 벽에 못을 못 박는 집이라 커튼을 포기했다가 이걸 찾았거든요…"
> ✅ **"못 하나 안 박고 커튼 달았어요."** + 나머지는 설치하는 손으로 보여줌

컷 설계할 때 **[발화]** 와 **[화면]** 을 나눠 적어라.

## 2-F. 재창작 하드룰

> 가져오는 것은 **구조**뿐이다 — 훅 방식, 장면 순서, 컷 리듬, 앵글 패턴, 설득 흐름, 총 길이.
>
> 절대 안 가져오는 것 — 원본 대사 문장, 자막, 음악, 영상 클립, 등장인물 얼굴, 브랜드·로고.
>
> 결과물은 "같은 구성인데 다른 영상"이어야지 "같은 영상"이면 안 된다.

## 2-G. 제품·니치 감지와 트렌드 리서치 (기획 단계)

**묻지 말고 먼저 파악한다.** 제품 링크나 이미지가 오면 조용히 도출한다 —
카테고리, 변형/옵션, 타깃 데모(2030 여성 · 자취생 · 신혼부부 · 가성비 지향 등),
니치 키워드(예: "자취 살림템", "홈카페 용품", "여름 주방템").

사용자에게는 질문이 아니라 **한 줄 상태 보고**로 넘어간다.
"포맷 5종", "균등 분배" 같은 내부 용어는 노출하지 않는다.

> "확인했어요 — 여름 주방템 쪽이네요. 국내 2030 타깃으로, 요즘 쇼핑 쇼츠에서 실제로 잘 먹히는
> 결로 가볼게요."

**트렌드 리서치 쿼리** (`[니치]`, `[연월]` 치환. WebSearch 로 돌린다. 쿼리를 사용자에게 나열하지 않는다):

1. `[니치] 틱톡 쇼핑 쇼츠 트렌드 [연월]`
2. `[니치] 인스타 릴스 바이럴 [연월]`
3. `[니치] 내돈내산 리뷰 인기 [연월]`
4. `[니치] 언박싱 하울 틱톡 [연월]`
5. `[니치] UGC 커머스 콘텐츠 트렌드 [연월]`
6. `[니치] 스크롤 멈추는 훅 [연월]`

결과에서 뽑을 것: 포맷 · 훅 패턴 · 비주얼 스타일 · 반응.
가장 유용한 페이지 2개는 본문까지 읽는다. **원문을 베끼지 않고 패턴·구조만** 참고해 새로 쓴다.
경쟁 브랜드명은 내부 참고용 — 영상이나 카피에 노출하지 않는다.

**기획 없이 바로 만들지 마라.** 리서치 → 브리프 → 승인을 거친다. 조회수는 거기서 갈린다.

**사용자에게 보이는 플랜은 카피 7종만.** 컷 구성·소스 매핑·타이밍 같은 내부 값은
`remix.json` 에 들고만 있고 플랜에 늘어놓지 않는다.

## 2-H. 여러 상품을 한 번에

링크를 2개 이상 주거나 "이 상품들로 각각 만들어줘" 류 요청이면:

1. 개수를 확인하고 **상품당 몇 개** 만들지 버튼으로 묻는다 (기본값: 상품당 1개)
2. **상품별로 리서치를 따로 돌린다.** 상품이 다르면 불만·후킹·경쟁 지점도 다르다.
   한 번 조사해서 돌려쓰지 않는다
3. **소재를 섞지 않는다.** 상품 A 영상이 상품 B 컷에 들어가면 안 된다.
   `sources/` 를 상품별 하위 폴더로 나눈다
4. 카피 킷도 **상품별 섹션**으로 나눠 낸다

## 2-I. 소재 균형 — 한 영상을 그대로 튼 것처럼 보이면 실패

리믹스는 **여러 소재를 교차**해야 리믹스다. 실제로 이런 일이 있었다:

> 비트마다 "내용에 맞는 장면"만 고르다 보니 한 소재가 **20컷 중 16컷(80%)** 을 먹었다.
> 게다가 **7컷(10초)이 연속**으로 같은 소재라, 보는 사람에게는 "그 영상을 그대로 틀고
> 나레이션만 얹은 것"으로 읽혔다.

**기준 두 가지 (`check_render.py` 가 자동 검사):**

| 항목 | 상한 |
|---|---|
| 한 소재의 컷 점유율 | **75% 이하** |
| 같은 소재 연속 컷 | **5컷 이하** (약 7초) |

**비율보다 연속이 더 중요하다.** 58/42 라도 앞부분이 10초 내리 한 소재면 티가 난다.

**균형을 못 맞출 때:** 대개 한 소재에만 핵심 장면(문제 상황·설치·성능 테스트)이 있어서다.
그럴 땐 두 가지로 푼다.

1. **해당 구간을 짧게.** 문제 제기를 3문장 → 2문장으로 줄이면 연속 구간이 그만큼 준다.
2. **한 비트 안에서 소재를 섞는다.** 예: 설치 비트를 `[설치 장면(데모), 설치된 모습(실사용)]`
   으로 짜면 같은 내용을 두 소재로 이어 말할 수 있다. 이게 레퍼런스들이 쓰는 A롤/B롤 구조다.

## 2-J. 대본 구조 — 나열하지 말고 기승전결로

**기능을 늘어놓으면 컷이 바뀌어도 같은 말로 들린다.** 실제로 이런 대본을 냈다가 지적받았다:

> ❌ "드라이기는 쓱 걸어두면 끝이고요, / 선 감아두니까 깔끔하고, /
>    위 수납함엔 화장품도 올려두고, / 빗도 같이 꽂아두니까 편하고요,"
>
> 같은 층위의 문장이 5개 연속. 진행이 없어서 지루하다.

**네 마디로 짠다:**

예시 문장을 베끼지 마라. **틀만 가져가고 말은 제품에서 새로 뽑는다.**

| | 하는 일 | 틀 |
|---|---|---|
| **기** | 훅 + **내** 문제 | "…〈제품〉 사봤는데요, 원래 〈예전 방식〉이라 〈불편〉했는데," |
| **승** | 해결되는 과정 | "〈사용법〉만 하면 끝이더라고요. 〈결과〉가 한 번에 〈해결〉됐어요." |
| **전** | **의심 → 해소** | "근데 〈의심되는 점〉 싶었는데, 〈증명 장면〉 하더라고요." |
| **결** | 결과 + CTA | "덕분에 〈최종 상태〉가 됐어요. 링크 아래에 두고 갈게요." |

`〈 〉` 안은 **1단계에서 실제로 본 장면**으로 채운다. 소재에 없는 동작을 넣지 마라.

**핵심은 전(轉)이다.** 성능·내구성 항목을 그냥 나열하면 또 하나의 기능일 뿐이지만,
앞에 **"근데 ~하지 않을까 싶었는데,"** 를 붙이면 시청자가 속으로 하던 질문을 대신 던져주는
장치가 된다. 그게 끝까지 보게 만든다.

**연결어를 써라.** `근데 / 그래서 / 덕분에 / 그러다` — 이게 없으면 문장이 병렬로 나열된다.

### 마지막 문장은 뚝 끊기지 않게

문장 끝을 강하게 내리면 "딱 끊긴" 느낌이 난다. 어미별로 실측한 값이다.

| 마무리 | 끝 하강 |
|---|---|
| "…링크는 아래에 **걸어둘게요.**" | -8.8 반음 ✗ |
| "…궁금하면 아래 링크 **보고 가세요.**" | -6.3 |
| "…링크는 아래에 **있어요~**" | -1.5 ★ |
| "…**만족했거든요.** 링크 아래에 **두고 갈게요.**" | **-0.5** ★ |

**앞 문장을 `~거든요`(상승)로 받쳐주면 마지막이 안 떨어진다.**
`~할게요`처럼 의지형으로 딱 끝내면 가장 크게 떨어진다.
`make_remix.py` 가 마지막 문장을 보고 받침 없이 `~게요/~세요`로 끝나면 경고한다.

**같은 어미를 세 문장 이어 쓰지 마라.** "~더라고요"만 세 번 나오면 리듬이 사라진다.
이것도 도구가 몇 번째 문장인지 찍어서 경고한다.

## 3. 절대 하지 말 것

1. **첫 컷에 CTA 넣기.** 6편 중 이걸 한 영상이 조회수 꼴찌권이었다.
2. **질문형 훅.** "…알고 계신가요?"는 스크롤을 멈추지 못한다.
2-B. **지시대명사 훅.** "이렇게…"로 시작하면 뭘 가리키는지 모른다. 2-A 참조.
2-C. **판매원 톤.** "~입니다/~할 수 있습니다"는 상세페이지 문장이다. §1 참조.
3. **기능 나열.** 용도를 늘어놓기만 하면 컷이 바뀌어도 같은 말로 들린다. 2-J 참조.
4. **없는 수치 지어내기.** 가격·시간·온도는 실제 측정값만. 최저가·효능 표현 금지.
5. **정적 이미지 슬라이드.** 러닝타임의 70% 이상을 실제 동영상으로 채운다.
6. **한 소재 몰아쓰기.** 2-I 참조 — 점유율 75% 초과 또는 연속 5컷 초과면 리믹스로 안 보인다.
7. **토킹헤드.** 사람이 제품 들고 설명하는 구성은 기본값이 아니다. 제품이 주인공이다.
8. **영상만 주고 끝내기.** 업로드 카피 7종(2-D)을 같이 내놓는다.

## 4. 컷 리듬

6편의 컷 길이를 전부 실측했다. 히트작도 실패작도 **컷 중앙값 1.24~1.77초**로 똑같다.
즉 리듬은 성패를 가르지 않지만 이 대역을 벗어나면 안 된다.

**대역은 중앙값에 걸린다. 컷 하나하나가 아니다.** 실측 영상에도 0.8초짜리와 2.4초짜리는
섞여 있다 — 강조 컷은 길고 연타 컷은 짧다. 모든 컷을 1.45초로 맞추면 오히려 기계적으로 들린다.

| 무엇 | 기준 |
|---|---|
| 컷 **중앙값** | 1.2~1.8초 |
| **개별** 컷 | 0.6~2.5초 (밖으로 나가면 그 비트의 장면 수가 틀린 것) |

**컷 수는 화면이 정한다** — `컷 수 = Σ(비트별 장면 수)`. 장면 하나가 컷 하나다.
**컷 길이는 말이 정한다** — `그 문장 길이 ÷ 그 비트의 장면 수`.
자세한 이유는 §6-B 「장면이 컷 수를 정한다」.

## 5. 진행 순서 — 8단계, 순서를 바꾸지 마라

> **왜 순서가 중요한가.** 예전에 대본을 먼저 쓰고 화면을 나중에 찾다가, 소재에 없는 장면을
> 요구하는 대본이 나왔다. 라벨을 채우라는 지시도 건너뛰고 레퍼런스 구조로 대본부터 썼다.
> 결과는 대본·음성·화면이 따로 노는 영상이었다. **화면을 먼저 보고 대본을 쓴다.**

### 0. 제품·니치 감지 + 트렌드 리서치
2-G 참조. 제품에서 카테고리·타깃·니치를 조용히 도출하고 트렌드를 검색한다.
사용자에게는 한 줄 상태 보고만. **기획 없이 바로 만들지 않는다.**
> 같은 제품으로 이어서 작업 중이면 건너뛰어도 된다. 새 제품이면 반드시 한다.

### 1. 🚧 레퍼런스 분석 게이트 — `watch` → `ref_sheet.py`

    python ~/.claude/skills/watch/scripts/watch.py <url> --out-dir <dir> --max-frames 20
    python ref_sheet.py --parent <상위폴더> --glob "r[0-9]"    # 컷 시트로 합치기

> **자막만 읽고 넘어가지 마라.** `watch` 는 프레임을 폴더에 흩뿌려 놓기만 해서,
> 실제로 대본만 읽고 화면을 한 번도 안 본 채 4단계까지 간 적이 있다.
> 그때 "레퍼런스는 접착 패드 붙이는 컷을 쓰는데 우리 소재엔 없다"는 걸 3단계 늦게 발견했다.

`ref_sheet.py` 가 만든 시트를 **전부 열어서 보고** 아래 표를 채운다:

| 항목 | 왜 필요한가 |
|---|---|
| 컷 수 · 컷 길이 | §4 대역 확인, 우리 컷 수 기준 |
| 컷 순서 (무엇 → 무엇) | 우리 비트 순서의 뼈대 |
| 제품 등장 시점 | 훅에 제품을 언제 넣을지 |
| 앵글 · 줌 · 크롭 | 어떤 그림이 필요한지 |
| A롤/B롤 구조 | 스튜디오 컷과 실사용 컷을 섞는지 |
| 원고 · CTA 위치 | §2, §3 대조 |

그리고 **반드시 마지막에 이걸 뽑는다:**

> ### ⛔ "우리 소재에 없는 컷" 목록
> 레퍼런스가 쓰는 컷 중 우리가 못 찍는 것을 전부 적는다.
> **여기서 소재 부족을 잡아야 2~4단계를 헛돌지 않는다.**
> 핵심 소구점을 증명할 컷이 없으면 **여기서 멈추고 소재를 더 받아라.**

> 레퍼런스가 있으면 5종 포맷 분배를 쓰지 않는다. 그 영상의 구조가 곧 포맷이다.

### 2. 소재 전처리 — `python prep_sources.py --dir raw --out sources`
원본은 `raw/`, 전처리본은 `sources/` 로 분리한다.
9:16 이 아닌 소재는 여기서 lanczos + 언샤프로 **1080x1920 을 미리 구워둔다.**
원본에 박힌 자막은 `--keep-top <px>` 로 잘라낸다 (잘라낼 높이는 **원본에서 실측**).

    python prep_sources.py raw/a.mp4 --keep-top 1070 --out sources

> 브라우저가 확대하게 두면 흐려진다. 저해상도(720p 미만) 소재는 확대해도 살아나지 않으니
> 아예 빼는 편이 낫다.

> **⛔ 소재 총길이 < 목표 길이 면 멈춰라.** 부족분만큼 같은 화면을 재사용하게 된다.
> 그건 리믹스가 아니라 늘리기다. 특히 **소재가 원테이크 1개면 리믹스 자체가 성립하지 않는다**
> — 컷을 섞을 상대가 없고, 순서를 바꾸면 점프컷이 티난다.
> 소재를 더 받거나, 목표 길이를 소재 길이에 맞춰 줄여라.

### 3. 인덱싱 + 스토리보드 — `python index_sources.py --dir sources --cuts 20`
장면전환으로 나누되 **후보 프레임 수를 보장**한다:
`max(소재 총길이 × 1장/초, 목표 컷수 × 1.5)`.
컷이 20개면 후보가 30장은 있어야 고를 여지가 생긴다. 롱테이크는 장면전환이 없어
검출만으로는 한두 장밖에 안 나오므로 이 보정이 없으면 무너진다.

산출: `out/storyboard.png` (번호 박힌 시트) + `out/scene_index.json`

### 4. 🚧 라벨 게이트 — 전부 채우기 전에는 대본으로 못 넘어간다

**못 쓰는 장면은 라벨을 `[`로 시작하라.** `[자막] 콜라 붓기`, `[파란컵] 주무르기` 처럼.
원본 자막이 박힌 구간, 다른 색·다른 제품이 나오는 구간이 여기 해당한다.
`make_remix.py` 가 이 표시를 읽어 **컷이 그 구간을 지나가면 경고한다.**
`out/storyboard.png` 를 **한 장으로 읽고**(낱장으로 읽으면 토큰 낭비) 모든 scene 의
`label` 을 채운다. 그다음 라벨을 **내러티브 비트로 묶고**, 소재에 **없는 장면 목록**을 적는다.

    보유: 완성 퍼올림 3장 · 냉동 보관 3장 · 빈 컵 내부 2장 · 붓기 3장 · 주무르기 2장 …
    없음: 제품 개봉 · 먹는 리액션 · 편의점 비교

> **빈 라벨이 하나라도 있으면 5단계로 넘어가지 마라.** 이 게이트를 건너뛰면
> 소재에 없는 장면을 요구하는 대본이 나온다.

### 5. 대본 — 비트 순서에 맞춰 쓴다
4단계의 비트 순서가 곧 대본 순서다. **없는 장면을 요구하는 문장은 쓰지 않는다.**
말투는 §1, 구조는 §2, 분량은 1.2배속 기준 **70어절 ≈ 29초**.

### 6. 컷 매핑 → `remix.json` — **비트 = 문장, 1:1**

`remix.json` 에는 컷이 아니라 **비트**를 적는다. 컷은 장면에서 계산된다.

```json
"beats": [
  { "name": "기 — 훅",     "shots": [30, 7] },
  { "name": "승 — 붙이면 끝", "shots": [15] }
]
```

**규칙은 셋뿐이다.**

1. **비트 개수 = 나레이션 문장 개수.** 다르면 `make_remix.py` 가 그 자리에서 멈춘다.
   비트 N 은 문장 N 을 말하는 동안 보여줄 화면이다.
2. **`shots` 에 적은 장면 수 = 그 문장이 가질 컷 수.** 순환도 채우기도 없다.
   적은 장면이 적은 순서대로 1:1 로 나온다.
3. **장면 수는 문장 길이로 정한다** — `문장 길이 ÷ 장면 수` 가 0.6~2.5초에 들어와야 한다.
   1.2배속 기준 대략:

   | 문장 길이 | 장면 |
   |---|---|
   | ~1.5초 | 1개 |
   | 1.5~2.6초 | 1~2개 |
   | 2.6~4.0초 | 2개 |
   | 4.0초 초과 | 3개 (문장을 쪼개는 쪽이 낫다) |

   틀리면 경고가 **몇 개로 맞추라고 숫자까지 알려준다.** 그대로 고치면 된다.

**대본 분량은 소재가 정한다.** 라벨을 채운 장면이 15개면 문장은 8~11개다.
장면이 모자라면 대본을 줄이는 게 맞다 — **채우기용 장면을 끼워넣지 마라**(§6-B).

### 7. 나레이션·자막·렌더

    python make_remix.py
    npx remotion render src/index.jsx Remix output/완성본.mp4 --crf 18

`make_remix.py` 가 한 번에 하는 일 — 손댈 필요 없다:

1. 대본 **전체를 1회 호출**로 읽힘 (문장별로 부르면 억양이 문장마다 리셋된다)
2. 글자 단위 타임스탬프 확보 → 문장·자막 조각 위치
3. **긴 침묵 압축** (`maxGap`)
4. 소재를 `public/sources/` 로 **자동 동기화**
5. **컷 = 장면 1:1** 배치, 컷 길이 = 문장 길이 ÷ 그 비트의 장면 수
6. 자막 조각을 글자 시각에 찍고 **공백 없이 연결**
7. 훅·톤·소재 검사 → 문제가 있으면 `[경고]` 출력

**대본과 목소리 설정이 지난번과 같으면 TTS 를 다시 부르지 않는다.**
무료 크레딧을 검증하느라 태우지 않기 위해서다. 강제로 다시 받으려면 `--force-tts`,
음성을 아예 건너뛰려면 `--no-tts`(대본 검사만 돌릴 때).

**TTS 가 실패하면 거기서 멈춘다.** 음성 없는 영상을 최종본으로 렌더하지 않는다.
**`[경고]` 가 뜨면 렌더 전에 먼저 해결하라.**

### 8. 검수 + 업로드 카피 — `python check_render.py output/완성본.mp4`
16항목 자동 확인: **썸네일 문구 있음** · **썸네일 이미지 최신** · 길이 · 해상도 · 오디오 트랙 · 오디오 길이 일치 · 용량 ·
**검은 프레임 없음** ·
**컷 중앙값 1.2~1.8초** · **0.6초 미만·2.5초 초과 컷 없음** ·
**연속 중복 장면** · **소재 편중** · **같은 소재 연속** ·
**자막↔음성 같은 대본** · **문장 수 일치** · **나레이션 최신 여부**. 그리고
검수 시트를 뽑는다. **시트를 눈으로 확인한다** — 자막 크기, 단어 중간 잘림, 원본 자막 잔상.
통과하면 2-D 의 카피 7종을 `output/카피킷.md` 로 낸다.

## 6. 기술 함정 — 전부 실제로 당한 것들

**나레이션은 `public/` 에 쓴다.** Remotion 은 `staticFile()` 로 `public/` 을 읽는다.
`out/` 에만 쓰고 `public/` 은 수동 복사하면, 대본을 고쳐도 **옛 음성이 그대로 렌더된다.**
자막은 최신이고 음성만 옛것이라 발견이 늦다. `make_remix.py` 는 `public/` 에 직접 쓰도록 고쳐뒀다.
렌더 전에 `public/narration.mp3` 의 길이를 확인하는 습관을 들여라.

**ElevenLabs 배속은 1.2 가 상한이다.** 1.3 을 요청하면 HTTP 400 이 난다.
1.2 로 생성한 뒤 ffmpeg `atempo` 로 나머지를 곱한다(음정 유지). `make_remix.py` 에 구현돼 있다.

**자막은 컷이 아니라 나레이션에서 만든다.** 컷에 자막을 매달면 들리는 말과 보이는 글자가
어긋난다. 문장을 16자 이하로 자르고 실제 음성 길이에 비례 배치한다.

**자막 조각은 균등 분배한다.** 앞에서부터 꽉 채우면 마지막에 "땡기잖아요" 같은 짧은 토막이 남는다.
한 절의 어절을 조각 수로 균등하게 나눠라.

**한국어 줄바꿈은 `word-break: keep-all`.** 없으면 "슬러시가 됩 / 니다"처럼 단어 중간이 잘린다.
글자수에 따라 폰트 크기도 낮춰라 (16자 초과 56px / 10자 초과 66px / 그 외 82px).

**영상 길이를 나레이션에 맞춘다.** TTS 길이를 잰 뒤 컷 길이를 비례 조정한다.
대본만 고치면 길이·자막이 따라오므로 수동 계산이 사라진다.

**소재를 브라우저에 확대시키지 마라.** 9:16 이 아닌 소재(예: 1080x1440)를 Remotion 에 그대로 넣으면
`objectFit: cover` 가 1.33배, 하단 크롭까지 걸면 1.75배까지 확대돼 눈에 띄게 흐려진다.
**ffmpeg 로 미리 1080x1920 을 구워서** 넣어라. 크롭·확대·샤프닝을 한 번에 처리하면 브라우저는 1.00배로 표시만 한다.

```bash
ffmpeg -i src.mp4 -vf "crop=1080:1070:0:0,scale=1938:1920:flags=lanczos,crop=1080:1920:429:0,unsharp=5:5:0.9:5:5:0.0"   -c:v libx264 -preset slow -crf 15 -pix_fmt yuv420p -an out_hi.mp4
```

첫 `crop` 이 원본 자막을 잘라내고, `scale=lanczos` 가 브라우저 기본 보간보다 선명하며, `unsharp` 이 확대로 잃은 윤곽을 살린다.
자르는 높이는 **원본에서 자막 위치를 실측해서** 정하라 — 여유 없이 자르면 잔상이 남는다.

**자막은 글자 단위 시각으로 찍는다.** 문장을 조각으로 나눈 뒤 **글자수 비례로 시간을 배분하면
문장 뒤쪽 조각이 밀린다.** ElevenLabs `with-timestamps` 가 글자마다 시각을 주므로 그걸 쓴다.
추정 → 실측으로 바꾸니 평균 오차가 **+0.009초**가 됐다.

**자막 사이에 공백을 두지 마라.** 조각 사이가 비면 음성은 이어지는데 자막만 사라졌다 뜬다.
그게 "안 맞는다"는 인상을 만든다. 각 조각의 끝을 **다음 조각 직전까지** 늘려라
(평균 공백 0.21초 → 0.02초).

**문장 사이 긴 침묵은 잘라낸다.** TTS 는 1초가 넘는 침묵을 만들기도 한다.
레퍼런스 실측 쉼은 평균 0.30초·최대 0.60초다. `tts_whole.compress_silence()` 가
무음만 줄이므로 억양·발음에는 영향이 없다. `remix.json` 의 `maxGap`(기본 0.32초)로 조절한다.

**분량 환산:** 1.2배속 기준 **한국어 70어절 ≈ 29초.** 25~30초를 노리면 65~72어절.

## 6-B. 반복해서 터진 버그의 유형 — 손으로 정한 값이 어긋난다

지금까지 난 사고는 **전부 같은 유형**이었다.
**서로 의존하는 두 값을 따로 정할 수 있게 열어둔 것.**

| 사고 | 어긋난 두 값 | 조치 |
|---|---|---|
| 옛 음성이 렌더됨 | `out/narration.mp3` ↔ `public/narration.mp3` | `public/` 에 직접 쓴다 |
| 컷 길이 1.10초 | 컷 수(손) ↔ 나레이션 길이(손) | 컷 수를 **장면 수에서 계산**한다 |
| 옛 소재가 렌더될 뻔 | `sources/` ↔ `public/sources/` | `make_remix.py` 가 **자동 동기화** |
| 자막 잔상 두 번 | `--keep-top` 실측값 | 자른 뒤 **확인 시트 강제 생성** |
| 소재 부족을 4단계에 발견 | 레퍼런스가 쓰는 컷 ↔ 우리 소재 | 1단계에 **"없는 컷" 게이트** |
| 소재 1개로 28초 시도 | 소재 총길이 ↔ 목표 길이 | 2단계에서 **길이 비교 후 중단** |
| 하중 문장이 방수 화면에 얹힘 | 나레이션 타임라인 ↔ 컷 타임라인 | **장면 ↔ 컷 1:1** (§6-B) |
| 물통 문장에 엉뚱한 컷 | 검사 통과용 채우기 장면 | 데이터를 조작하지 않는다 (§6-B) |
| 크레딧을 검증에 태움 | 같은 대본을 매번 TTS | 대본·설정이 같으면 **재사용** |

### 값이 아니라 **일**을 건너뛴 사고

위는 값이 어긋난 경우고, 아래는 **해야 할 일을 안 한** 경우다. 더 위험하다.

| 사고 | 왜 생겼나 | 조치 |
|---|---|---|
| 라벨을 안 채우고 대본부터 씀 | 4단계에 게이트가 없었다 | 라벨 게이트 |
| **레퍼런스 자막만 읽고 화면을 안 봄** | 1단계에 게이트가 없었다. 프레임은 뽑혀 있었는데 폴더에 흩어져 있어 안 열어봤다 | `ref_sheet.py` + 1단계 게이트 |

**공통점: 텍스트는 싸고 이미지는 비싸서, 이미지 보는 일을 건너뛴다.**
그래서 두 게이트 모두 **시트 한 장으로 합쳐 강제로 보게** 만든다.
낱장으로 읽으라고 하면 토큰이 아까워 결국 안 본다.

### 검증 코드 자체가 죽어 있던 사고

`problems` 리스트를 **초기화하지 않은 채** `problems.append(...)` 만 쓰고 있었다.
게다가 마지막에 출력하는 코드도 없었다. 결과적으로:

- 검사에 다 통과하는 동안에는 아무 일도 없었다
- **문제를 발견하는 순간 `NameError` 로 크래시**했다
- 즉 "경고를 띄운다"고 문서에 적어둔 장치가 실제로는 한 번도 동작한 적이 없었다

**교훈: 검증 코드는 반드시 실패하는 입력으로 한 번 돌려봐라.**
통과만 확인하면 검증기가 죽어 있어도 모른다.

### 장면이 컷 수를 정한다 — 거꾸로 하면 반드시 어긋난다

이 파이프라인에서 가장 여러 번 터진 곳이다. 두 번 잘못 만들었다.

**1차 (틀림): 대본이 컷 수를 정했다.** `컷 수 = 나레이션 ÷ 1.45초` 로 균일하게 깔고,
각 컷은 "그 시각에 말하는 문장의 비트"에서 장면을 **순환**해 골랐다.
컷 수와 장면 수가 안 맞으니 장면이 돌아가며 재사용됐다. 결과:
**"생수병 매달아도 꿈쩍 안 해요" 위에 드라이어 컷이 떴다.**
컷 수를 화면이 아니라 말이 정했기 때문이다.

**2차 (맞음): 화면이 컷 수를 정한다.**

```
컷 수     = Σ(비트별 장면 수)      ← 화면이 정한다
컷 길이   = 문장 길이 ÷ 그 비트의 장면 수   ← 말이 정한다
장면 ↔ 컷 = 1:1, 순환 없음
```

컷과 장면이 1:1 이므로 **채우기용 장면이 아예 필요 없다.** 문장 위에는 그 문장을 위해
고른 장면만 뜬다. 길이가 대역을 벗어나면 그건 리듬 문제가 아니라
**그 비트의 장면 수가 틀렸다는 신호**이고, 경고가 몇 개로 고치라고 알려준다.

> **왜 균일 분할로는 안 됐나** — 1차의 근거는 "문장이 1.8~2.4초면 §4 대역(1.2~1.8초)을
> 만족하는 분할이 없다"였다. 이건 대역을 잘못 읽은 것이다. 실측값은 컷 **중앙값**이지
> 모든 컷의 범위가 아니다. 개별 컷은 0.6~2.5초면 된다. 잘못 읽은 제약 하나가
> 아키텍처를 통째로 뒤집어 놓았다. **기준을 코드로 옮기기 전에 그 기준이 평균값인지
> 개별값인지부터 확인하라.**

### 컷은 장면보다 길다 — 옆 장면으로 넘어간다

장면 ↔ 컷은 1:1 이지만 **길이까지 같지는 않다.** 장면이 0.7초인데 컷이 1.9초면
그 컷은 다음 장면까지 1.2초를 더 재생한다. 같은 소재가 이어지니 보통은 자연스럽다.
문제는 넘어간 곳이 **못 쓰는 구간**일 때다.

실제로 그렇게 나왔다. 원본 중국어 자막이 없는 장면을 골랐는데, 컷이 길어 옆의
자막 있는 장면까지 재생해 **완성본에 중국어가 그대로 떴다.** 자동 검사 13개가 전부
통과했고, 검수 시트를 눈으로 보고서야 발견했다.

- 4단계에서 못 쓰는 장면은 라벨을 `[` 로 시작해 표시한다
- `make_remix.py` 가 각 컷의 재생 구간 `[in, in+dur]` 이 그 구간을 스치는지 검사한다
- 걸리면 **비트에 장면을 더 넣어 컷을 줄이거나** 다른 장면을 쓴다

소재의 "깨끗한 창"이 컷 길이의 상한이다. 예: 자막 장면 사이에 낀 0.36초짜리 장면은
어떤 컷에도 못 쓴다. 라벨 게이트에서 창 길이를 먼저 재라.

### 컷이 소재 파일 끝을 넘으면 검게 나온다

컷 시작점을 장면 기준으로만 잡으면 `시작 + 길이` 가 파일 길이를 넘을 수 있다.
넘어간 만큼은 검은 화면이다. `make_remix.py` 가 파일 길이에서 당겨 준다.

### 프레임이 안 맞물리면 경계마다 검은 프레임이 낀다

`Remix.jsx` 가 `from = round(start×fps)` 와 `dur = round(dur×fps)` 를 **따로** 반올림해서,
`from + dur ≠ 다음 from` 인 경계에 1프레임 구멍이 났다. 그 프레임은 검게 렌더된다.
0.03초라 눈으로는 놓치고, 검수 시트가 하필 그 프레임을 잡아야 보인다.

**해법: 다음 컷의 시작 프레임에서 역산한다.** `dur = 다음 from − 이번 from`.
그리고 `check_render.py` 가 `blackdetect` 로 전 구간을 기계로 확인한다.

### 게이트는 문서가 아니라 도구가 막아야 한다

0단계(제품·니치·트렌드)와 1단계(레퍼런스 분석)를 건너뛰고 3단계 스토리보드만 보고
대본을 쓴 적이 있다. 결과는 **화면에 없는 동작을 말하는 대본**이었다 —
"냉동실에 넣어뒀거든요"라고 했는데 소재에는 **꺼내는 장면만** 있었다.
문서에 🚧 라고 적어둬도 건너뛴다. 그래서 `make_remix.py` 가 `out/refs/` 가 비어 있으면
**실행을 거부한다**(`--skip-ref` 로만 해제).

1단계에서 반드시 손에 남겨야 할 것:

| 항목 | 왜 |
|---|---|
| 소재별 0~1초에 뭐가 나오는지 | 훅을 뭘로 열지 결정한다 |
| 동작 순서와 시각 | **대본의 각 문장이 이 순서를 따라야 한다** |
| 결과 장면(머니샷) 위치 | 훅과 CTA 에 둘 다 쓴다 |
| **없는 컷 목록** | 여기 없는 동작은 대본에 쓰면 안 된다 |

마지막 줄이 핵심이다. **소재에 없는 동작은 문장으로도 쓰지 마라.**

### 장면 경계와 원본 자막이 사라지는 시점은 다르다

`[자막]` 장면을 피해 컷을 잡았는데도 완성본에 중국어가 떴다. 재보니 자막이
장면 경계보다 **0.42초 더 남아** 있었다. 장면 검출은 화면 변화를 보지 자막을 보지 않는다.
`blockedMargin`(기본 0.5초)만큼 넓혀 잡고, 걸리면 컷 시작을 **자동으로 밀어낸다.**

### 사용자가 준 소재는 전부 쓴다 — 임의로 빼지 마라

소재 3개를 받고 그중 하나를 뺀 적이 있다. 이유는 있었다 — 제품 색이 다르고 배경이
지저분했다. 그래도 **틀렸다.** 소재를 몇 개 쓸지는 사용자가 정하는 것이고,
3개를 준 건 3개를 쓰라는 뜻이다.

빼야 할 이유가 보이면 **빼지 말고 말해라.** 그리고 일단 다 쓴 버전을 만들어라.
빼는 건 사용자가 "빼라"고 한 다음이다.

실제로 3개를 다 쓰니 소재 비중이 39/33/28% 로 오히려 균형이 좋아졌다.
2개일 때는 67/33 이었다 — 한쪽이 두 배였다. 내가 걱정한 색 차이보다
**한 영상을 길게 트는 느낌**이 시청자에게 더 크게 걸린다.

### 검사를 통과시키려고 데이터를 조작하지 마라

"연속 중복 장면 없음" 검사가 걸렸을 때, 비트에 상관없는 장면 `#49` 를 하나 끼워
검사를 통과시킨 적이 있다. 검사는 초록불이 됐고, **영상은 물통 문장에 엉뚱한 화면이 떴다.**
그 채우기 장면이 바로 위 사고의 직접 원인이었다.

- 검사 실패는 **소재나 대본이 부족하다는 신호**다. 부족한 걸 채워라.
- 부족한 걸 못 채우겠으면 **대본을 줄여라.** 소재에 없는 얘기는 하지 않는 게 맞다.
- 숫자를 맞추려고 `remix.json` 에 의미 없는 값을 넣는 순간, 검사는 통과하고 영상은 망가진다.
- 검사를 고치는 것도 같은 함정이다. 임계값을 낮추기 전에 **그 임계값이 어디서 나온
  실측인지** 먼저 확인하라 (위 「왜 균일 분할로는 안 됐나」).

### 출력을 grep 으로 걸러 보지 마라 — 트레이스백이 숨는다

`check_render.py` 를 `grep -E "PASS|FAIL"` 로 걸러 보고 있었다. 전부 PASS 였다.
그런데 스크립트는 PASS 를 다 찍은 **직후 시트 생성에서 죽고 있었다.**
`for a, b2 in zip(...)` 이 argparse 의 `a` 를 덮어써서 `a.shots` 가 터진 것이다.
grep 이 트레이스백을 지웠고, **8단계의 눈 확인 게이트가 통째로 건너뛰어졌다.**
그 사이 중국어 자막과 검은 프레임이 완성본에 들어갔다.

- 도구 출력은 **마지막 줄까지** 본다. 걸러야 하면 종료 코드를 같이 확인하라
- 산출물이 나와야 하는 단계는 **파일 존재를 검사해서 없으면 실패**시킨다
  (`sheet.png` 가 없으면 `check_render.py` 는 이제 에러로 끝난다)
- 루프 변수에 한 글자 이름을 쓰지 마라. 바깥 이름을 조용히 덮어쓴다

### 검증 코드는 실패하는 입력으로 한 번 돌려봐라

경고를 모으는 리스트를 초기화하지 않아, **첫 진짜 문제에서 검증기 자체가 죽은** 적이 있다.
정상 입력만 넣어보면 영원히 안 걸린다. 새 검사를 넣었으면 **일부러 틀린 `remix.json`**
으로 한 번 돌려서 그 검사가 실제로 말을 하는지 확인하라. 지금 기준으로 최소 이 네 가지:

| 일부러 넣는 오류 | 나와야 하는 반응 |
|---|---|
| 비트 개수 ≠ 문장 개수 | 즉시 중단 |
| 한 비트에 장면 과다 → 컷 0.6초 미만 | 경고 + 몇 개로 맞출지 숫자 |
| 인덱스에 없는 장면 번호 | 즉시 중단 |
| 합쇼체(`~합니다`)로 끝나는 문장 | 경고 |
| 컷이 `[...]` 라벨 구간을 지나가게 | 경고 + 어느 구간인지 |
| 컷이 소재 파일 끝을 넘게 | 시작점을 자동으로 당김 |

### 도구에 제품 정보를 넣지 마라

**틀은 바뀌면 안 된다.** 제품마다 달라지는 값은 전부 `remix.json` 에 있고,
`*.py` 와 `src/Remix.jsx` 는 어떤 제품에도 그대로 돌아가야 한다.

실제로 이런 걸 넣었다가 잡혔다:

    key = [w for w in prod.replace("무타공", "").split() ...]   # ← 이번 제품 전용

이번 영상이 잘 나오게 하려고 도구를 만지면, 다음 제품에서 조용히 어긋난다.
**영상이 이상하면 `remix.json` 을 고쳐라.** 도구를 고치는 건 모든 제품에 해당하는
문제일 때만이다.

새 제품으로 넘어가기 전에 반드시 확인:

    grep -nE "제품명|고유명사" *.py     # 도구에 제품 흔적이 없어야 한다
    python new_project.py --archive <이전> --name "<새 제품>"

**규칙: 값이 다른 값에서 유도될 수 있으면 손으로 적게 두지 마라.**
새 설정을 추가할 때마다 "이 값이 다른 값과 어긋날 수 있나?"를 먼저 물어라.

현재 자동화된 것: 컷 수 · 컷 길이 · 영상 길이 · 비트 가중치(=shots 개수) ·
소재 동기화 · 나레이션 경로 · 자막 타이밍.

현재도 손으로 정하는 것 (어긋나면 경고가 뜨도록 해둠):
- `--keep-top` — 원본 자막 위치. **실측하고 확인 시트를 반드시 본다**
- `narration` 문장 — 비트당 1~2문장을 벗어나면 경고
- `caption.size/bottom` — 검수 시트를 눈으로 보고 조정
- `speed` — 사용자 지정값

## 7. 소재와 권리

우선순위: ① 사용자가 직접 촬영/제공한 동영상 ② 사용자가 권리를 가진 소재 ③ 이미지(보조).

**레퍼런스 영상은 분석 전용이다.** 권한이 확인되지 않은 제3자 영상은 프레임 한 장도 최종본에
넣지 않는다. 워터마크 유무는 무관하다 — 로고가 없어도 저작권은 있고, Content ID 는 로고가 아니라
영상 자체를 대조한다. 크롭·확대·순서 변경으로 회피되지 않는다.

소재가 없다고 보고만 하고 멈추지 마라. 판매자에게 홍보 소재를 요청하는 경로(중국 소싱이면
`无字幕原视频` / `无水印` 로 요청)를 안내하고, 그동안 대본·컷 구조·TTS 는 미리 만들어 둔다.

**원본에 자막이 박혀 있으면** `cropBottom` 으로 하단을 잘라낸다(0.24 전후). 로고가 구석이면
ffmpeg `delogo`. 화면 중앙에 큰 글씨면 그 구간은 버린다.

## 8. 비용 규칙

허용: ElevenLabs TTS(유료 플랜 가입됨) · watch · yt-dlp · ffmpeg · ffprobe · Remotion **로컬** 렌더 · 로컬 도구.
금지: Higgsfield · Runway · Kling · Veo · 유료 AI 영상 생성 · Remotion Cloud · PAYG · Auto Top Up.

ElevenLabs 를 외부 API라는 이유로 건너뛰지 마라. 키가 있으면 무료 크레딧 범위에서 생성한다.
현재 키는 권한이 TTS 로 제한돼 **잔여 크레딧을 API 로 조회할 수 없다**(`user_read` 401).
생성 성공 여부로 판단하고, 402 나 크레딧 소진 응답이 오면 그때 멈추고 보고한다.
결제·자동충전은 어떤 경우에도 켜지 않는다.

**TTS 가 실패하면 거기서 멈춘다. 음성 없는 영상을 최종본으로 렌더하지 않는다.**

## 9. 자막 폰트

캡컷 감성의 둥근 "아기 폰트"는 **Jua(배달의민족 주아체)** 가 가장 가깝다. Google Fonts 무료.
`public/fonts/` 에 ttf 를 받아 두고 Remotion 에서 `FontFace` + `delayRender` 로 로딩한다
(그냥 `@font-face` 만 쓰면 렌더가 폰트를 기다리지 않아 기본 폰트로 나갈 수 있다).

받아둔 후보와 용도:

| 폰트 | 느낌 | 쇼츠 자막 적합도 |
|---|---|---|
| **Jua** | 둥글고 두툼함 | ★ 기본값 |
| BlackHanSans | 각지고 매우 굵음 | 임팩트용 |
| Dongle | 둥글지만 획이 얇음 | 부적합 |
| Gaegu · GamjaFlower · HiMelody | 손글씨 | 획이 얇아 부적합 |

**주아체는 같은 px 에서 시각적으로 작게 보인다.** 맑은 고딕 기준보다 8~12px 키워라.
현재 값: 16자 초과 62px / 10자 초과 74px / 그 외 90px.

광고표시 같은 **고지 문구는 주아체 말고 맑은 고딕**으로 둔다 (가독성 우선).

### 자막 스타일 규칙

- **크기는 고정한다.** 글자수에 따라 폰트 크기를 바꾸면 자막이 커졌다 작아졌다 해서 조잡해 보인다.
  대신 **조각을 짧게 잘라** 한 줄에 들어가게 하라. 1080 폭 기준 **94px / 조각 최대 12자**가 기준값
  (화면 폭의 75~85% 를 차지한다).
- **외곽선은 `-webkit-text-stroke` 로.** `text-shadow` 를 네 방향으로 겹쳐 흉내내면 모서리가 계단처럼
  깨져 보인다. 굵은 검정 stroke 레이어 위에 흰 글자 레이어를 정확히 겹쳐 그려라.
- **주아체에 `font-weight: bold` 를 주지 마라.** 단일 굵기 폰트라 브라우저가 합성 볼드를 만들면서
  획이 뭉갠다. `font-weight: 400` 고정.
- **줄바꿈은 균형 분배.** 앞에서부터 꽉 채우면 마지막에 "번만" 같은 토막이 남는다.
  필요한 최소 조각 수를 구한 뒤, 그 수를 유지하는 최소 용량을 이분탐색해서 배치한다
  (`make_remix.py` 의 `_wrap_balanced`).


### 썸네일은 별도 이미지다 — 영상 본편에 문구를 얹지 마라

**썸네일은 영상을 누르기 전에 보이는 그림이다.** 영상 안이 아니다.
문구를 본편에 얹으면 재생 내내 남아 나레이션 자막과 위아래로 겹친다.
실제로 3.14초 동안 얹어놓고 "썸네일을 넣었다"고 한 적이 있다 — 그건 자막이지 썸네일이 아니다.

    python thumb.py            # 첫 컷 + 문구 → output/썸네일.png
    python thumb.py --cut 5    # 다른 컷으로
    python thumb.py --at 12.3  # 그 시각이 든 컷으로

- 영상 본편에는 문구가 **안 들어간다** (`headline.inVideo` 기본 `false`)
- 썸네일에는 나레이션 자막을 넣지 않는다. 문구 하나만 읽히게 한다
- 업로드할 때 이 이미지를 커버/썸네일로 지정한다

**첫 컷은 머니샷으로 잡는다.** `thumb.py` 가 기본으로 1번 컷을 쓰고,
플랫폼이 첫 프레임을 자동으로 고르는 경우에도 그 그림이 잡힌다.

#### 고정 규격 — 제품이 바뀌어도 그대로 둔다

```json
"headline": {
  "text": "",          // 제품마다 새로 뽑는다. 아래 '문구는 제품마다 새로 뽑는다' 참조
  "cut": 1,            // 어느 컷 위에 얹을지. 고른 뒤 반드시 적어둔다
  "inVideo": false,
  "style": "box", "boxColor": "#0D0D0D", "accent": "#FFD84D",
  "size": 118, "top": 330, "stroke": 22
}
```

| 값 | 이유 |
|---|---|
| `inVideo: false` | 썸네일 전용. 본편에 얹으면 자막과 겹친다 |
| `style: "box"` | 검정 박스가 밝은 화면·어두운 화면 어디서나 똑같이 읽힌다 |
| `boxColor: "#0D0D0D"` | 색 4종을 실제로 렌더해 비교한 결과. 와인·남색은 배경에 묻힌다 |
| `accent: "#FFD84D"` | 박스가 검정이라 노랑이 가장 선명하다 |
| `size: 118` / `top: 330` | 1080x1920 기준. 광고 배지 아래, 제품 위 |

**표기법** — `|` 로 줄바꿈, `*단어*` 로 그 단어만 `accent` 색.
제품마다 바뀌는 건 **`text` 한 줄뿐이다.**

#### 문구는 제품마다 새로 뽑는다 — 지난 문구를 가져다 쓰지 마라

**여기 적힌 문장을 복사하지 마라.** 아래는 뽑는 **절차**지 문구 목록이 아니다.
같은 문장이 다음 제품에 또 나오면 채널이 템플릿처럼 보인다.

**1) 1단계 분석에서 재료를 꺼낸다**

| 재료 | 어디서 |
|---|---|
| 문제 장면 | 이 제품이 없을 때의 불편이 찍힌 컷 |
| 결과 장면 | 쓰고 난 뒤의 상태 |
| 증명 장면 | 못 믿을 만한 걸 실제로 해 보이는 컷 |

**2) 각도를 바꿔 후보를 4~6개 만든다.** 하나만 만들지 마라.

| 각도 | 만드는 법 | 세기 |
|---|---|---|
| **문제 찌르기** | 시청자가 지금 하고 있는 습관을 묻는다 | ★★★ |
| **불가능해 보이기** | 그림이 말을 증명하는데 말이 안 될 것 같은 것 | ★★★ |
| **의심** | "이게 되나?" 를 대신 말해준다 | ★★ |
| **결과** | "~했더니 ~됐다" | ★ 궁금할 게 없다 |

**3) 후보마다 어울리는 컷을 붙여 실제로 렌더한다.**

    python thumb.py --cut 12 --out out/tn/01.png
    python thumb.py --cut 3  --out out/tn/02.png

**그리고 나란히 붙여서 눈으로 고른다.** 글로만 고르면 반드시 틀린다.

**4) 고르는 기준**

| 기준 | 확인 |
|---|---|
| **궁금증이 생기는가** | 결과만 말하면 멈출 이유가 없다 |
| **문구+그림이 합쳐서 무슨 카테고리인지 아는가** | 문구에도 그림에도 제품이 없으면 딴 물건으로 읽힌다 |
| 확인 안 된 숫자가 없는가 | 재보지 않은 시간·수치를 쓰지 마라 |
| 두 줄 · 한 줄 8~10자 · 강조 한 곳 | 118px 기준 |

두 번째 기준은 실제로 틀렸던 항목이다. 증명 장면(무거운 물건을 매단 컷)에
그 물건 이름을 문구로 붙였더니 **그 물건의 거치대로 읽혔다.**
증명 각도를 쓸 때는 문구나 그림 한쪽에 반드시 제품이 남아 있어야 한다.

**5) 고른 컷 번호를 `headline.cut` 에 적는다.** 안 적으면 `thumb.py` 를 다시 돌릴 때
1번 컷으로 돌아가 다른 썸네일이 나온다.

> `make_remix.py` 가 보관된 제품들의 문구와 대조해서, 같은 문구를 다시 쓰면 경고한다.

### CTA 는 "댓글" 로 통일한다 — 영상 하나로 세 곳에 올린다

`"링크 아래에 두고 갈게요"` 라고 읽혔던 적이 있다. 유튜브에서만 맞는 말이다.
**인스타·틱톡은 본문에 클릭되는 링크가 안 걸린다.** 같은 영상을 세 곳에 올리므로
나레이션은 어디서나 맞는 말이어야 한다.

    ✅ "…거든요. 구매라고 댓글 남겨주시면 링크 보내드릴게요."
    ❌ "…거든요. 링크 아래에 두고 갈게요."

`구매` 같은 **키워드를 영상에서 말하고**, 인스타 자동 DM 키워드를 그것과 똑같이 맞춘다.

| 플랫폼 | 링크를 어디에 | 카피킷에 적을 것 |
|---|---|---|
| 유튜브 쇼츠 | 설명란에 직접 | 설명 + **고정 댓글** |
| 인스타 릴스 | 프로필 상단 링크 | 본문 + **댓글 자동 DM 키워드** |
| 틱톡 | 프로필 바이오 | 설명 + **댓글 답글** |

### 파트너스 고지는 복사 블록 **안에** 넣는다

유튜브 설명에만 넣고 틱톡·인스타 블록에는 빠뜨린 적이 있다.
체크리스트에 "세 곳 모두 넣어라"고 적어둬도, **복사하는 건 블록이지 체크리스트가 아니다.**

- 카피킷의 **모든 플랫폼 블록 끝**에 고지 문구를 넣는다
- **자동 DM 메시지와 댓글 답글에도** 넣는다 (링크를 보내는 곳마다)
- `check_render.py` 가 카피킷을 열어 블록 수만큼 고지가 있는지 센다

고지 문구는 이것 하나로 통일한다:

    쿠팡 파트너스 활동을 통해 일정액의 수수료를 제공받습니다.

### 카피킷에는 업로드 규칙과 금지사항을 함께 넣는다

문구만 주고 끝내면 올리는 사람이 실수한다. **카피킷 끝에 항상 이 세 절을 넣는다.**

**1) 표시 위치와 플랫폼 토글** — 본문 고지만으로는 부족하다.

| 플랫폼 | 본문 고지 외에 추가로 켤 것 |
|---|---|
| 유튜브 | 업로드 화면 → "유료 프로모션 포함" 체크 |
| 인스타 | 고급 설정 → "유료 파트너십 라벨 추가" |
| 틱톡 | 게시 화면 → "브랜디드 콘텐츠" 토글 |

고지는 **소비자가 쉽게 볼 수 있는 곳**에 있어야 한다. 본문 맨 아래 "더보기"에 숨으면
표시하지 않은 것으로 본다. 한국어로 쓴다 — `AD` `PR` `Sponsored` `#sp` 단독은 안 된다.

**2) 하지 말아야 할 것**

- 고지를 지우고 올리기 / 링크만 바꾸고 고지를 빠뜨리기
- 자동 DM·댓글 답글에 고지 빠뜨리기 — **링크가 가는 모든 곳**에 필요하다
- 확인 안 된 숫자(가격·용량·시간·온도) 쓰기
- `최저가` `1위` `유일한` `100%` — 근거 없이 쓰면 표시광고법 위반
- 건강·미용 제품에 효능을 단정하기
- 권한 확인 안 된 남의 영상을 최종본에 넣기
- 지난 제품 문구 재사용 (도구가 대조해서 경고한다)
- 영상 CTA 키워드와 자동 DM 키워드를 다르게 두기

**3) 올리기 전 체크리스트** — 링크 채움 · 세 플랫폼 본문 고지 · 플랫폼 토글 ·
자동 DM 고지 · CTA 키워드 일치 · 미확인 수치 없음.

> 체크리스트에 `[쿠팡_링크]` 같은 **자리표시자 문자열을 그대로 쓰지 마라.**
> `check_render.py` 가 "링크 안 채움"으로 오탐한다. "링크 자리를 전부 바꿨는가" 로 쓴다.

### 광고 표시는 영상 내내 보여야 한다

제휴·협찬 영상은 **상시 표시**가 원칙이다(공정위 추천·보증 심사지침).
마지막 2초에만 고지를 띄우면 그 앞을 본 사람은 광고인 줄 모른다.

`src/Remix.jsx` 의 `AdBadge` 가 전 구간에 배지를 그린다. 설정은 `remix.json`:

```json
"adBadge": {
  "text": "광고", "position": "topRight",
  "size": 34, "margin": 44,
  "bgOpacity": 0.45, "textOpacity": 0.95
}
```

- `position` — `topRight`(기본) · `topLeft` · `bottomRight` · `bottomLeft`.
  **상단을 쓴다.** 하단은 유튜브 쇼츠 UI(제목·버튼)와 자막이 차지한다
- `size` 34 / `margin` 44 가 1080x1920 기준값. 화면을 가리지 않으면서 읽힌다
- 배경을 반투명(0.45)으로 깔아야 밝은 화면에서도 읽힌다
- `"adBadge": false` 로 끌 수 있지만, **제휴 링크를 거는 영상에서는 끄지 마라**

마지막 컷의 전체 고지문(`[광고] 쿠팡 파트너스 활동의 일환으로…`)은 그대로 둔다.
배지는 상시 표시, 마지막 고지문은 법정 문구 — **둘 다 필요하다.**

## 10. 목소리 — 한국어 원어민만 쓴다

**영어권 보이스로 한국어를 읽히지 마라.** 어미와 받침이 뭉개진다.
ElevenLabs 기본 보이스 16개는 전부 영어권이라 한국어 쇼츠에 쓰면 안 된다.

| 성별 | voice ID | 비고 |
|---|---|---|
| 여자 | `QPFsEL6IBxlT15xfiD6C` | 기본값 |
| 남자 | `m3gJBS8OofDJfycyA2Ip` | 남성 화자가 필요할 때 |

`tts.py` 의 `KR` 딕셔너리에 등록돼 있다. `remix.json` 에 `"voice": "여자"` 또는 `"남자"` 만 쓰면 된다.

> **유료 플랜이 필요하다.** 이 둘은 보이스 라이브러리 소속이라 무료 플랜에서는 **HTTP 402**
> (`Free users cannot use library voices via the API`) 로 막힌다. 무료로 돌릴 땐 영어권
> 기본 보이스를 쓸 수밖에 없고, 그러면 발음이 어색한 걸 감수해야 한다.

### 나레이션 실측 기준 — 어색하면 여기부터 재라

"영혼이 없다", "끊긴다" 같은 말을 들으면 감으로 고치지 말고 **레퍼런스와 수치로 비교**하라.
레퍼런스 3편에서 뽑은 기준값이다.

| 항목 | 레퍼런스 | 재는 법 |
|---|---|---|
| 중앙 F0 | 200~242Hz | 자기상관 피치 추적 |
| **음높이 변화폭** | **9.7~14.3 반음** | 상위10%/하위10% 비율 |
| **쉼 평균** | **0.29~0.30초** | RMS 무음 구간 |
| **쉼 최대** | **0.40~0.60초** | 〃 |
| 문장 끝 하강 | -1.7~-4.3 반음 | 무음 직전 0.4초 vs 그 앞 |
| 강조 빈도 | 초당 1.3~5.3회 | 중앙+4dB 넘는 국소 최대 |
| 글자/초 | 7.1~8.5 | 대본 글자수 ÷ 길이 |
| 음량 변화폭 | 16~17dB | 상위90%-하위10% |

**실제로 이렇게 진단했다:**

1. 속도·쉼 개수·음량·강조는 처음부터 레퍼런스와 거의 같았다 → 원인이 아니다
2. **음높이 변화폭만 5.5 반음**으로 절반이었다 → `style` 파라미터를 안 쓴 탓
3. 고친 뒤에도 "끊긴다"는 말이 나와 다시 재보니 **쉼 최대가 1.28초** (레퍼런스는 0.60초)

**문장 끝 하강은 정상이다.** 레퍼런스도 -1.7~-4.3 반음 떨어진다.
하강이 문제라고 지레짐작하고 연결어미로 대본을 뜯어고쳤지만 효과는 작았다.
**재보고 나서 고쳐라.**

**긴 침묵은 잘라낸다.** `tts_whole.compress_silence()` 가 무음만 줄이므로
억양·발음에는 영향이 없다. `remix.json` 의 `maxGap`(기본 0.32초)로 조절한다.

### 억양이 평탄하면 "영혼이 없다"고 느껴진다

한국어 나레이션은 음높이가 크게 오르내린다. 이게 없으면 문장이 토막처럼 들린다.

**실측 기준값 (레퍼런스 3편):** 중앙 F0 **200~242Hz** / 음높이 변화폭 **9.7~14.3 반음**

실제로 이런 일이 있었다:

| | 중앙 F0 | 변화폭 | |
|---|---|---|---|
| 영어권 보이스 기본 (stability 0.5) | 205Hz | **5.5 반음** | 레퍼런스의 절반 — 평탄 |
| 영어권 + stability 0.2 + **style 0.8** | 242Hz | 12.7 반음 | 대역 안 |
| 한국어 여성 (stability 0.35, style 0.5) | 258Hz | 9.7 반음 | 대역 안 |

**`style` 파라미터를 안 쓰면 억양이 눌린다.** `stability` 만 낮추는 건 효과가 거의 없었다
(0.5→0.3 에서 5.8, 오히려 나빠짐). `style` 을 0.5 이상 주는 게 핵심이다.

속도·쉼·음량 다이내믹은 기본값으로도 레퍼런스와 거의 같았다. **음높이만 달랐다.**
톤이 어색하다는 말을 들으면 먼저 F0 변화폭을 재라.

## 11. `remix.json` 전체 설정

제품이 바뀌면 **이 파일만** 새로 쓰면 된다. 컷은 적지 않는다 — 계산된다.

```json
{
  "title": "제품명 쇼핑쇼츠 REMIX",
  "product": "드라이기 거치대",        // 훅에 이게 들어갔는지 검사한다
  "keySelling": "구멍",                // 핵심 소구 키워드. 훅이나 2번째 문장에 있어야 한다

  "fps": 30, "width": 1080, "height": 1920,

  "voice": "여자",                     // 또는 "남자". tts.py 의 KR 딕셔너리
  "voiceSettings": { "stability": 0.35, "similarity": 0.75, "style": 0.5 },
  "speed": 1.2,                        // API 상한 1.2. 초과분은 ffmpeg atempo
  "maxGap": 0.32,                      // 문장 사이 침묵 상한(초)
  "gap": 0.12, "tail": 0.35,           // 문장 간격 / 끝 여유

  "font": "Jua",
  "caption": { "size": 94, "bottom": 330, "stroke": 17, "punch": 1.12 },

  "sceneIndex": "out/scene_index.json",
  "targetCutSec": 1.45,                // 장면 수가 틀렸을 때 "몇 개로 고쳐라" 를 계산하는 기준값.
                                       // 컷 수는 여기서 나오지 않는다 — 장면 수가 정한다

  "narration": [ { "text": "…" } ],    // 기승전결. 비트와 1:1
  "beats": [ { "name": "기 — 훅", "shots": [30, 7] } ]   // shots = 4단계 라벨 번호
}
```

**손대면 안 되는 것:** `cuts` 를 직접 쓰지 마라. `autoTiming` 은 남아 있지만 지금은
글자 타임스탬프를 쓰므로 무시된다.

**반드시 지킬 것:** `narration` 문장 수 == `beats` 개수. 1:1 이어야 말과 화면이 붙는다.

## 12. 관련 파일

`Desktop\쇼츠` 폴더. **폴더 역할을 섞지 마라.**

| 경로 | 역할 |
|---|---|
| `raw/` | 받은 원본 그대로. 손대지 않는다 |
| `sources/` | 전처리된 1080x1920. 인덱싱·컷 매핑은 여기 기준 |
| `public/sources/` | Remotion 이 읽는 사본. **`make_remix.py` 가 자동 동기화** — 손으로 복사하지 마라 |
| `public/narration.mp3` | 나레이션 **단일 출처.** `make_remix.py` 가 직접 쓴다 |
| `public/fonts/` | Jua 외 5종 |
| `out/` | 중간 산출물 — 레퍼런스 컷 시트 · 스토리보드 · 인덱스 · 자막 · 검수 시트 |
| `output/` | 최종 영상 + 카피킷 |
| `projects/<제품명>/` | 끝난 제품의 소재·산출물 보관. **새 제품 시작 전에 옮긴다** |

도구 (단계 순):

| 파일 | 단계 | 하는 일 |
|---|---|---|
| `new_project.py` | 0 | 이전 제품 보관 + 초기화. **손으로 옮기지 마라** |
| `ref_sheet.py` | 1 | watch 프레임을 번호 박힌 컷 시트로 합침 |
| `prep_sources.py` | 2 | 1080x1920 으로 굽기, 원본 자막 크롭, 크롭 확인 시트 |
| `index_sources.py` | 3 | 장면 인덱싱 + 스토리보드 시트 (후보 장수 보장) |
| `remix.json` | 5~6 | 대본과 **비트**(문장 1개 = 비트 1개). 컷은 여기 적지 않는다 |
| `make_remix.py` | 6~7 | 컷 배치(장면 1:1) · TTS · 자막 · 소재 동기화 · 검사 · props 생성 |
| `tts_whole.py` | 7 | 대본 전체 1회 호출 + 글자 타임스탬프 + 침묵 압축 |
| `src/Remix.jsx` | 7 | 컷 배치 · 크롭 · punch-in · 자막 · CTA |
| `tts.py` | 7 | ElevenLabs 호출 (`--list` 로 보이스 목록) |
| `check_render.py` | 8 | 완성본 16항목 자동 검수 + 검수 시트 |

`프롬프트_템플릿.md` — 사용자가 새 작업을 시작할 때 붙여넣는 요청 양식.
8단계를 빠짐없이 돌리고 게이트에서 멈추게 하는 문구가 들어 있다.

**새 제품 시작 / 지난 제품 복구 — 손으로 옮기지 마라:**

    python new_project.py --archive <이전제품명> --name "<새 제품명>"     # 보관 후 새로 시작
    python new_project.py --archive <지금것> --restore <지난제품명>        # 지난 제품으로 되돌리기

`--restore` 는 `projects/<이름>` 을 작업 폴더로 통째로 되돌린다. 지금 작업 중인 상태가
남아 있으면 `--archive` 를 함께 주지 않는 한 **거부한다** — 덮어써서 잃지 않기 위해서다.

손으로 폴더를 옮기면 반드시 빠뜨린다. 실제로 `public/narration.mp3` 를 안 옮겨서
**자막은 새 제품, 음성은 이전 제품**인 상태가 나왔다. 더 나쁜 건 검수를 통과했다는 점이다 —
음성과 영상 길이는 서로 맞았으니까. 어긋난 건 `spans.json` 과 음성이었다.

`new_project.py` 는 제품마다 달라지는 것을 **한 목록으로 관리**한다:
`raw · sources · out · output · public/sources · public/narration.mp3 · remix.json`.
새 항목이 생기면 그 목록에만 추가하면 된다.

**다른 제품으로 만들 때는 `remix.json` 의 `narration` 과 `beats` 만 새로 쓰면 된다.**
